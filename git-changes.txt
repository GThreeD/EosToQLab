=== GIT STATUS ===
 M .github/workflows/macos-build.yml
 M EosToQLab.slnx
 M docs/ARCHITECTURE.md
 M docs/BUILD_AND_TEST.md
 M docs/TESTING.md
 M docs/VALIDATION.md
 M src/EosToQLab.Application/Components/Layout/MainLayout.razor
 M src/EosToQLab.Application/Components/Pages/Home.razor
 M src/EosToQLab.Application/MauiProgram.cs
 M src/EosToQLab.Application/Platforms/MacCatalyst/Info.plist
 M src/EosToQLab.Application/Resources/AppIcon/appicon.svg
R  src/EosToQLab.Core/Diagnostics/Esf3dFollowNotDecodedWarning.cs -> src/EosToQLab.Core/Diagnostics/EosShowArchiveFollowNotDecodedWarning.cs
A  src/EosToQLab.Core/Diagnostics/EosShowArchiveLossTolerantParsingWarning.cs
R  src/EosToQLab.Core/Diagnostics/Esf3dNoCueSequenceWarning.cs -> src/EosToQLab.Core/Diagnostics/EosShowArchiveNoCueSequenceWarning.cs
A  src/EosToQLab.Core/Diagnostics/EosShowArchiveVersionNotTestedWarning.cs
D  src/EosToQLab.Core/Diagnostics/Esf3dLossTolerantParsingWarning.cs
A  src/EosToQLab.Core/Exceptions/EosShowArchiveInvalidException.cs
D  src/EosToQLab.Core/Exceptions/Esf3dArchiveInvalidException.cs
 M src/EosToQLab.Core/Exceptions/ShowDataEntryMissingException.cs
 M src/EosToQLab.Core/Exceptions/ShowDataReadException.cs
 M src/EosToQLab.Core/Exceptions/UnsupportedEosImportFormatException.cs
A  src/EosToQLab.Core/Import/EosShowArchiveCompatibility.cs
A  src/EosToQLab.Core/Import/IEosShowArchiveCompatibility.cs
 M src/EosToQLab.Core/Models/EosSourceKind.cs
 M src/EosToQLab.Infrastructure/EosToQLab.Infrastructure.csproj
A  src/EosToQLab.Infrastructure/Import/EosShowArchive/EmbeddedEosShowArchiveCompatibility.cs
R  src/EosToQLab.Infrastructure/Import/Esf3d/Esf3dCueHeaderDecoder.cs -> src/EosToQLab.Infrastructure/Import/EosShowArchive/EosShowArchiveCueHeaderDecoder.cs
R  src/EosToQLab.Infrastructure/Import/Esf3d/Esf3dEosCueImporter.cs -> src/EosToQLab.Infrastructure/Import/EosShowArchive/EosShowArchiveCueImporter.cs
 M tests/EosToQLab.Tests/.editorconfig
R  tests/EosToQLab.Tests/Core/Diagnostics/Esf3dFollowNotDecodedWarningTests.cs -> tests/EosToQLab.Tests/Core/Diagnostics/EosShowArchiveFollowNotDecodedWarningTests.cs
R  tests/EosToQLab.Tests/Core/Diagnostics/Esf3dLossTolerantParsingWarningTests.cs -> tests/EosToQLab.Tests/Core/Diagnostics/EosShowArchiveLossTolerantParsingWarningTests.cs
R  tests/EosToQLab.Tests/Core/Diagnostics/Esf3dNoCueSequenceWarningTests.cs -> tests/EosToQLab.Tests/Core/Diagnostics/EosShowArchiveNoCueSequenceWarningTests.cs
A  tests/EosToQLab.Tests/Core/Diagnostics/EosShowArchiveVersionNotTestedWarningTests.cs
R  tests/EosToQLab.Tests/Core/Exceptions/Esf3dArchiveInvalidExceptionTests.cs -> tests/EosToQLab.Tests/Core/Exceptions/EosShowArchiveInvalidExceptionTests.cs
 M tests/EosToQLab.Tests/Core/Exceptions/ShowDataEntryMissingExceptionTests.cs
 M tests/EosToQLab.Tests/Core/Exceptions/ShowDataReadExceptionTests.cs
AM tests/EosToQLab.Tests/Core/Import/EosShowArchiveCompatibilityTests.cs
A  tests/EosToQLab.Tests/Core/Import/IEosShowArchiveCompatibilityTests.cs
 M tests/EosToQLab.Tests/Core/Models/EosSourceKindTests.cs
 M tests/EosToQLab.Tests/EosToQLab.Tests.csproj
A  tests/EosToQLab.Tests/Fixtures/EosShowArchive/Compatibility/eos-3.3.5.69/README.md
AM tests/EosToQLab.Tests/Fixtures/EosShowArchive/Compatibility/eos-3.3.5.69/expected.json
R  tests/EosToQLab.Tests/Fixtures/Esf3d/known-3.3.5_Build_69/synthetic-known-3.3.5_Build_69.esf3d -> tests/EosToQLab.Tests/Fixtures/EosShowArchive/Compatibility/eos-3.3.5.69/parser-contract.esf3d
A  tests/EosToQLab.Tests/Fixtures/EosShowArchive/README.md
R  tests/EosToQLab.Tests/Fixtures/Esf3d/dense-cue-record-variants-3.3.5.esf3d -> tests/EosToQLab.Tests/Fixtures/EosShowArchive/Regression/dense-cue-records/dense-cue-record-variants-3.3.5.69.esf3d
R  tests/EosToQLab.Tests/Fixtures/Esf3d/known-3.3.5_Build_69-with-901-label/expected.json -> tests/EosToQLab.Tests/Fixtures/EosShowArchive/Regression/dense-cue-records/expected.json
AM tests/EosToQLab.Tests/Fixtures/EosShowArchive/Regression/real-cue-901-labelled/expected.json
R  tests/EosToQLab.Tests/Fixtures/Esf3d/known-3.3.5_Build_69-with-901-label/synthetic-known-3.3.5_Build_69-with-901-label.esf3d -> tests/EosToQLab.Tests/Fixtures/EosShowArchive/Regression/real-cue-901-labelled/synthetic-known-3.3.5_Build_69-with-901-label.esf3d
RM tests/EosToQLab.Tests/Fixtures/Esf3d/known-3.3.5_Build_69-with-901/expected.json -> tests/EosToQLab.Tests/Fixtures/EosShowArchive/Regression/real-cue-901/expected.json
R  tests/EosToQLab.Tests/Fixtures/Esf3d/known-3.3.5_Build_69-with-901/synthetic-known-3.3.5_Build_69-with-901.esf3d -> tests/EosToQLab.Tests/Fixtures/EosShowArchive/Regression/real-cue-901/synthetic-known-3.3.5_Build_69-with-901.esf3d
A  tests/EosToQLab.Tests/Fixtures/Esf3d/Every_declared_tested_version_has_a_versioned_golden_fixture.esf3d
D  tests/EosToQLab.Tests/Fixtures/Esf3d/README.md
RM tests/EosToQLab.Tests/Fixtures/Esf3d/known-3.3.5_Build_69/expected.json -> tests/EosToQLab.Tests/Fixtures/Esf3d/expected.json
 M tests/EosToQLab.Tests/GlobalUsings.cs
 M tests/EosToQLab.Tests/Infrastructure/Import/EosCueImporterFactoryTests.cs
AM tests/EosToQLab.Tests/Infrastructure/Import/EosShowArchive/EmbeddedEosShowArchiveCompatibilityTests.cs
R  tests/EosToQLab.Tests/Infrastructure/Import/Esf3d/Esf3dCueHeaderDecodeResultTests.cs -> tests/EosToQLab.Tests/Infrastructure/Import/EosShowArchive/EosShowArchiveCueHeaderDecodeResultTests.cs
A  tests/EosToQLab.Tests/Infrastructure/Import/EosShowArchive/EosShowArchiveCueHeaderDecoderTests.cs
RM tests/EosToQLab.Tests/Infrastructure/Import/Esf3d/Esf3dEosCueImporterTests.cs -> tests/EosToQLab.Tests/Infrastructure/Import/EosShowArchive/EosShowArchiveCueImporterTests.cs
D  tests/EosToQLab.Tests/Infrastructure/Import/Esf3d/Esf3dCueHeaderDecoderTests.cs
R  tests/EosToQLab.Tests/TestDoubles/Esf3dHeaderFixtureBuilder.cs -> tests/EosToQLab.Tests/TestDoubles/EosShowArchiveHeaderFixtureBuilder.cs
A  tests/EosToQLab.Tests/TestDoubles/FakeEosShowArchiveCompatibility.cs
A  tools/EosToQLab.CompatibilityCatalogGenerator/EosToQLab.CompatibilityCatalogGenerator.csproj
A  tools/EosToQLab.CompatibilityCatalogGenerator/Program.cs
?? git-changes.txt
?? src/EosToQLab.Infrastructure/eos-show-archive-tested-archives.json

=== UNSTAGED CHANGES ===
diff --git a/.github/workflows/macos-build.yml b/.github/workflows/macos-build.yml
index e41744b..17041ad 100644
--- a/.github/workflows/macos-build.yml
+++ b/.github/workflows/macos-build.yml
@@ -8,7 +8,7 @@ on:
 
 jobs:
   test:
-    name: xUnit and 100% line coverage
+    name: xUnit, fixture catalog, and 100% line coverage
     runs-on: ubuntu-latest
     steps:
       - uses: actions/checkout@v4
@@ -19,7 +19,7 @@ jobs:
           cache-dependency-path: tests/EosToQLab.Tests/EosToQLab.Tests.csproj
       - name: Restore tests
         run: dotnet restore tests/EosToQLab.Tests/EosToQLab.Tests.csproj
-      - name: Run xUnit tests and enforce coverage
+      - name: Generate fixture catalog, run xUnit tests, and enforce coverage
         run: |
           dotnet test tests/EosToQLab.Tests/EosToQLab.Tests.csproj \
             --configuration Release \
@@ -41,6 +41,7 @@ jobs:
           path: |
             artifacts/test-results
             artifacts/coverage
+            src/EosToQLab.Infrastructure/obj/Release/**/eos-show-archive-tested-archives.json
           if-no-files-found: warn
 
   publish:
diff --git a/EosToQLab.slnx b/EosToQLab.slnx
index 7764544..173bc16 100644
--- a/EosToQLab.slnx
+++ b/EosToQLab.slnx
@@ -14,4 +14,5 @@
     <Project Path="src/EosToQLab.Infrastructure/EosToQLab.Infrastructure.csproj"/>
     <Project Path="src\EosToQLab.Application\EosToQLab.Application.csproj"/>
     <Project Path="tests/EosToQLab.Tests/EosToQLab.Tests.csproj"/>
+    <Project Path="tools/EosToQLab.CompatibilityCatalogGenerator/EosToQLab.CompatibilityCatalogGenerator.csproj"/>
 </Solution>
diff --git a/docs/ARCHITECTURE.md b/docs/ARCHITECTURE.md
index 93b9df5..3a96639 100644
--- a/docs/ARCHITECTURE.md
+++ b/docs/ARCHITECTURE.md
@@ -6,7 +6,7 @@ The application uses a deliberately small architecture:
 
 - source formats are replaceable import strategies;
 - source parsers do not know about QLab;
-- QLab does not know whether a cue came from CSV or ESF3D;
+- QLab does not know whether a cue came from CSV or an EOS show archive;
 - the Blazor UI coordinates services but contains no parsing or OSC protocol logic.
 
 ## Projects
@@ -16,7 +16,7 @@ The application uses a deliberately small architecture:
 Contains stable application concepts:
 
 - `EosCue` and result models;
-- `IEosCueImporter` and `IEosCueImporterFactory`;
+- `IEosCueImporter`, `IEosCueImporterFactory`, and the storage-agnostic `IEosShowArchiveCompatibility`;
 - `IQLabService` as the application-facing facade;
 - `IQLabImportPlanBuilder`;
 - diagnostic and exception types.
@@ -28,7 +28,8 @@ It has no MAUI, archive, reflection-mapping, TCP, or JSON dependencies beyond th
 Contains implementation details:
 
 - `CsvEosCueImporter`;
-- `Esf3dEosCueImporter` plus `Esf3dCueHeaderDecoder` for cue notes and Follow/Hang metadata;
+- `EosShowArchiveCueImporter` plus `EosShowArchiveCueHeaderDecoder` for cue notes and Follow/Hang metadata;
+- `EmbeddedEosShowArchiveCompatibility`, which reads only the build-generated tested-archive resource;
 - `EosCueImporterFactory`;
 - OSC encoding/decoding;
 - double-END SLIP framing for QLab TCP;
@@ -39,6 +40,13 @@ Contains implementation details:
   communication;
 - centralized protocol names in `QLabProtocol`.
 
+
+### EosToQLab.CompatibilityCatalogGenerator
+
+A build-only console tool scans explicit compatibility fixtures, verifies each archive's `version.json` against its
+`expected.json`, and writes a deterministic JSON catalog into the Infrastructure intermediate output. The tool has no
+reference to Core, Infrastructure, or the test assembly. Regression and synthetic fixture directories are never scanned.
+
 ### EosToQLab.Application
 
 A macOS-only .NET MAUI Blazor Hybrid application. Razor components handle the workflow:
@@ -58,7 +66,7 @@ select source with native file picker
 ```text
 IEosCueImporterFactory
     ├── CsvEosCueImporter
-    └── Esf3dEosCueImporter
+    └── EosShowArchiveCueImporter
 ```
 
 Adding another EOS source format requires:
@@ -72,7 +80,7 @@ Adding another EOS source format requires:
 CSV parsing uses `EosCsvCue` as a source model because its shape follows an external format. Attributes identify the
 column for each property. The binder is generic and metadata is cached per type.
 
-After parsing and cue-part aggregation, source rows are mapped into `EosCue`. ESF3D recovery also maps directly into
+After parsing and cue-part aggregation, source rows are mapped into `EosCue`. EOS show archive recovery also maps directly into
 `EosCue`. Everything after that point uses only the common model.
 
 ## QLab import planning
diff --git a/docs/BUILD_AND_TEST.md b/docs/BUILD_AND_TEST.md
index f99f041..ca95beb 100644
--- a/docs/BUILD_AND_TEST.md
+++ b/docs/BUILD_AND_TEST.md
@@ -49,15 +49,14 @@ XAML-generated code, and the WebView host. Its pure state and workflow logic liv
 by xUnit; the shell itself is verified by the macOS build job and manual UI checks. This avoids claiming meaningful unit
 coverage for generated or operating-system-owned UI behavior.
 
-## ESF3D compatibility tests
+## EOS show archive compatibility tests
 
 No test requires an installed or running ETC Eos instance. Tests use two complementary fixture types:
 
 1. deterministic synthetic fixtures for every known binary encoding and parser error path;
 2. immutable, versioned golden-master exports with an `expected.json` contract.
 
-When a new Eos version changes `.esf3d`, add a minimal sanitized export under a new fixture directory rather than
-replacing an old fixture. See [TESTING.md](TESTING.md).
+When a new Eos version changes `.esf2` or `.esf3d`, add a minimal sanitized export under `Fixtures/EosShowArchive/Compatibility` rather than replacing an old fixture. The Infrastructure build derives its embedded tested-version catalog from those contracts, and the publish job runs only after the golden-master tests pass. See [TESTING.md](TESTING.md).
 
 ## Build the macOS app
 
@@ -90,6 +89,6 @@ dotnet build src/EosToQLab.Application/EosToQLab.Application.csproj \
 10. Test without an open workspace and verify `QLabNoOpenWorkspaceException`.
 11. Load the supplied Follow-chain sample and verify both dropdown modes: exclusion removes 83.2, 83.3, and 84; disarmed
     mode creates them with Armed off.
-12. Drag both `.csv` and `.esf3d` files onto the drop zone and also select them through the file dialog.
+12. Drag `.csv`, `.esf2`, and `.esf3d` files onto the drop zone and also select them through the file dialog.
 13. Inspect one generated EOS Network cue: Type `Cues`, Specify user `No`, Command `Run cue in specific list`, then the
     expected List and Cue values.
diff --git a/docs/TESTING.md b/docs/TESTING.md
index e18cea6..e60ddac 100644
--- a/docs/TESTING.md
+++ b/docs/TESTING.md
@@ -42,56 +42,61 @@ Tests/.../QLabOscSessionTests.cs
 `<TypeName>Tests` class or the `<TypeName>Tests.cs` file is missing. Interfaces, enums, records, record structs, static
 classes, internal protocol helpers, and exception types are included in the convention.
 
-## ESF3D without a live Eos dependency
+## EOS show archives without a live Eos dependency
 
-`.esf3d` is proprietary and may change between Eos releases. A live Eos installation should not be a prerequisite for
-deterministic CI, so the parser is tested using a compatibility corpus.
+The parser does not require an installed or running Eos instance in CI. Test data is divided by intent:
 
-### Synthetic fixtures
-
-Synthetic fixtures are generated byte-for-byte and cover known primitives independently:
+```text
+tests/EosToQLab.Tests/Fixtures/EosShowArchive/
+├── Compatibility/
+│   └── eos-<version>/
+│       ├── parser-contract.esf2 or parser-contract.esf3d
+│       ├── expected.json
+│       └── README.md
+├── Regression/
+└── Synthetic/
+```
 
-- tagged unsigned integers;
-- UTF-16 text fields;
-- null and boolean values;
-- legacy Follow/Hang text, numeric, boolean, and compact-object forms;
-- current continuation-object Follow/Hang forms;
-- malformed and truncated structures;
-- archive and `showdat.dat` error paths.
+Only `Compatibility/` is evidence that an archive identity is supported by a released build. Regression archives may
+reproduce a particular defect, and synthetic archives may exercise a binary branch, but neither category is advertised
+as a verified Eos version.
 
-These fixtures make parser regressions reproducible and allow precise failure localization.
+Each compatibility contract declares the identity independently in `expected.json`:
 
-### Versioned golden masters
+```json
+{
+  "archive": {
+    "format": "{...}",
+    "version": "3.3.5.69"
+  },
+  "cues": []
+}
+```
 
-A real export captures assumptions that a synthetic builder may miss. For each relevant Eos release, create a tiny
-sanitized show containing only purpose-built test cues and commit:
+The archive's own `version.json` must contain the same `Format` and `Version`. The build fails when the contract is
+missing, the archive manifest is unreadable, or the values disagree. `Format` and `Version` are treated as an opaque
+pair; the project does not assume that the GUID changes for every schema change.
 
-```text
-tests/EosToQLab.Tests/Fixtures/Esf3d/eos-<version>/
-├── parser-contract.esf3d
-├── expected.json
-└── README.md
-```
+### Build-generated runtime catalog
 
-The expected contract should include the Eos version and the intended cue number, label, notes, scene text, Follow/Hang,
-and list number. Never overwrite a committed fixture when Eos changes. Add a new directory, so CI keeps proving backward
-compatibility with every previously supported encoding.
+`EosToQLab.CompatibilityCatalogGenerator` scans only `Fixtures/EosShowArchive/Compatibility`. Before Infrastructure
+resources are prepared, MSBuild generates `eos-show-archive-tested-archives.json` into `obj` and embeds it in
+`EosToQLab.Infrastructure`. No tested-version list is maintained by hand in production code.
 
-### Upgrade workflow
+The same compatibility directories feed the parameterized golden-master parser test. The release workflow runs tests
+before either macOS package is published. Therefore an identity can reach the embedded runtime catalog only from a
+committed compatibility fixture, and a package is released only after that fixture's parser contract passes.
 
-When ETC releases a new Eos version:
+Tests never write production artifacts. Catalog generation is a deterministic build step, not an xUnit side effect.
 
-1. open the minimal parser-contract show in that Eos version;
-2. export a fresh `.esf3d`;
-3. remove unrelated show data and verify that no production names or notes remain;
-4. add it under a new versioned fixture directory;
-5. copy and update `expected.json`;
-6. run the xUnit suite;
-7. only change the parser when the new fixture fails;
-8. retain all older fixtures and add focused synthetic tests for the new binary shape.
+### Adding a new Eos version
 
-This leaves one deliberate manual compatibility capture per Eos release, but all subsequent verification is independent
-of Eos and runs in CI.
+1. Create a minimal artificial show containing labels, notes, scene text, fractional cue numbers, Follow, and Hang.
+2. Export one `.esf2` or `.esf3d` archive.
+3. Add a new directory under `Compatibility/`; never replace an older contract.
+4. Record the independently known expected cues and the archive identity in `expected.json`.
+5. Run the complete xUnit suite and inspect the generated catalog artifact.
+6. Keep malformed, unsupported, or bug-specific archives under `Regression/` or `Synthetic/`, not `Compatibility/`.
 
 ## Live integration boundaries
 
diff --git a/docs/VALIDATION.md b/docs/VALIDATION.md
index 0bb7309..6881057 100644
--- a/docs/VALIDATION.md
+++ b/docs/VALIDATION.md
@@ -5,11 +5,13 @@ The test migration adds these automated checks:
 - xUnit discovery through `Microsoft.NET.Test.Sdk`;
 - one dedicated test class and file for every top-level Core and Infrastructure type;
 - parser, planning, mapper, workflow, rollback, OSC codec, SLIP framing, TCP transport, JSON reply, and exception tests;
-- deterministic synthetic ESF3D tests;
-- an immutable ESF3D golden-master contract;
+- deterministic synthetic EOS show archive tests;
+- explicit compatibility, regression, and synthetic fixture categories;
+- an immutable EOS show archive golden-master contract;
+- a deterministic build-generated compatibility catalog embedded in Infrastructure;
 - a Coverlet Cobertura report;
 - a CI failure threshold of 100% line coverage for `EosToQLab.Core` and `EosToQLab.Infrastructure`;
-- macOS publishing only after the test job succeeds.
+- macOS publishing only after the golden-master tests and coverage job succeed.
 
 Validation performed in the generation environment:
 
diff --git a/src/EosToQLab.Application/Components/Layout/MainLayout.razor b/src/EosToQLab.Application/Components/Layout/MainLayout.razor
index 6573477..2c1d7fb 100644
--- a/src/EosToQLab.Application/Components/Layout/MainLayout.razor
+++ b/src/EosToQLab.Application/Components/Layout/MainLayout.razor
@@ -6,7 +6,7 @@
             <div class="eyebrow">EOS → QLab 5</div>
             <div class="brand">EosToQLab</div>
         </div>
-        <div class="header-note">CSV and ESF3D · OSC only · open workspaces</div>
+        <div class="header-note">CSV, ESF2, and ESF3D · OSC only · open workspaces</div>
     </header>
     <main class="content">@Body</main>
 </div>
diff --git a/src/EosToQLab.Application/Components/Pages/Home.razor b/src/EosToQLab.Application/Components/Pages/Home.razor
index 23cd3c8..fab66b7 100644
--- a/src/EosToQLab.Application/Components/Pages/Home.razor
+++ b/src/EosToQLab.Application/Components/Pages/Home.razor
@@ -11,7 +11,7 @@
     <div>
         <div class="eyebrow">Import workflow</div>
         <h1>Build a protected QLab cue list from EOS</h1>
-        <p>Choose an EOS CSV or ESF3D file, review and edit the parsed cues, and import them into a workspace that is
+        <p>Choose an EOS CSV, ESF2, or ESF3D file, review and edit the parsed cues, and import them into a workspace that is
             already open in QLab 5.</p>
     </div>
     <div class="status-pill @(_busy ? "working" : "ready")">
@@ -37,7 +37,7 @@
             <span class="step-number">1</span>
             <div>
                 <h2>EOS source</h2>
-                <p>CSV and ESF3D use separate import strategies behind the same interface.</p>
+                <p>CSV and EOS show archives use separate import strategies behind the same interface.</p>
             </div>
         </div>
 
@@ -45,7 +45,7 @@
             <InputFile OnChange="LoadSourceAsync" disabled="@_busy"/>
             <span class="drop-icon">⇩</span>
             <strong>Drop a file here or click to choose</strong>
-            <span>Accepted: .csv, .esf3d</span>
+            <span>Accepted: .csv, .esf2, .esf3d</span>
         </label>
 
         @if (_source is not null)
diff --git a/src/EosToQLab.Application/MauiProgram.cs b/src/EosToQLab.Application/MauiProgram.cs
index 7c148d2..5ab09ec 100644
--- a/src/EosToQLab.Application/MauiProgram.cs
+++ b/src/EosToQLab.Application/MauiProgram.cs
@@ -4,7 +4,7 @@ using EosToQLab.Core.QLab;
 using EosToQLab.Core.Security;
 using EosToQLab.Infrastructure.Import;
 using EosToQLab.Infrastructure.Import.Csv;
-using EosToQLab.Infrastructure.Import.Esf3d;
+using EosToQLab.Infrastructure.Import.EosShowArchive;
 using EosToQLab.Infrastructure.QLab;
 using EosToQLab.Infrastructure.QLab.Workflow;
 
@@ -24,8 +24,9 @@ public static class MauiProgram
         // Optional persistent implementation. Enable this registration only for a signed build
         // that is allowed to use the platform keychain, and remove the session registration above.
         // builder.Services.AddSingleton<IQLabPasscodeStore, MauiSecurePasscodeStore>();
+        builder.Services.AddSingleton<IEosShowArchiveCompatibility, EmbeddedEosShowArchiveCompatibility>();
         builder.Services.AddSingleton<IEosCueImporter, CsvEosCueImporter>();
-        builder.Services.AddSingleton<IEosCueImporter, Esf3dEosCueImporter>();
+        builder.Services.AddSingleton<IEosCueImporter, EosShowArchiveCueImporter>();
         builder.Services.AddSingleton<IEosCueImporterFactory, EosCueImporterFactory>();
         builder.Services.AddSingleton<IQLabImportPlanBuilder, QLabImportPlanBuilder>();
         builder.Services.AddSingleton<IQLabOscService, QLabOscService>();
diff --git a/src/EosToQLab.Application/Platforms/MacCatalyst/Info.plist b/src/EosToQLab.Application/Platforms/MacCatalyst/Info.plist
index 0343aca..5c816d6 100644
--- a/src/EosToQLab.Application/Platforms/MacCatalyst/Info.plist
+++ b/src/EosToQLab.Application/Platforms/MacCatalyst/Info.plist
@@ -34,6 +34,24 @@
                     <string>application/zip</string>
                 </dict>
             </dict>
+            <dict>
+                <key>UTTypeIdentifier</key>
+                <string>com.gthreed.eostoqlab.esf2</string>
+                <key>UTTypeDescription</key>
+                <string>ETC Eos ESF2 show archive</string>
+                <key>UTTypeConformsTo</key>
+                <array>
+                    <string>public.zip-archive</string>
+                    <string>public.data</string>
+                </array>
+                <key>UTTypeTagSpecification</key>
+                <dict>
+                    <key>public.filename-extension</key>
+                    <array><string>esf2</string></array>
+                    <key>public.mime-type</key>
+                    <string>application/zip</string>
+                </dict>
+            </dict>
         </array>
         <key>CFBundleDocumentTypes</key>
         <array>
@@ -69,6 +87,14 @@
                     <string>esf3d</string>
                 </array>
             </dict>
+            <dict>
+                <key>CFBundleTypeName</key><string>EOS ESF2</string>
+                <key>CFBundleTypeRole</key><string>Viewer</string>
+                <key>LSHandlerRank</key><string>Alternate</string>
+                <key>LSItemContentTypes</key>
+                <array><string>com.gthreed.eostoqlab.esf2</string></array>
+                <key>CFBundleTypeExtensions</key><array><string>esf2</string></array>
+            </dict>
         </array>
         <key>UIBackgroundModes</key>
         <array>
diff --git a/src/EosToQLab.Application/Resources/AppIcon/appicon.svg b/src/EosToQLab.Application/Resources/AppIcon/appicon.svg
index 18bfb9a..b331133 100644
--- a/src/EosToQLab.Application/Resources/AppIcon/appicon.svg
+++ b/src/EosToQLab.Application/Resources/AppIcon/appicon.svg
@@ -6,41 +6,41 @@
         id="svg3"
         xmlns="http://www.w3.org/2000/svg"
 >
-  <defs id="defs3"/>
-  <rect
-          x="0"
-          y="0"
-          width="1024"
-          height="1024"
-          rx="224"
-          fill="#6fd2ff"
-          id="outer-border"/>
+    <defs id="defs3"/>
+    <rect
+            x="0"
+            y="0"
+            width="1024"
+            height="1024"
+            rx="224"
+            fill="#6fd2ff"
+            id="outer-border"/>
 
-  <rect
-          x="24"
-          y="24"
-          width="976"
-          height="976"
-          rx="204"
-          fill="#262d3d"
-          id="app-surface"/>
+    <rect
+            x="24"
+            y="24"
+            width="976"
+            height="976"
+            rx="204"
+            fill="#262d3d"
+            id="app-surface"/>
 
-  <g transform="translate(512 512) scale(1.4) translate(-512 -512)">
-    <path
-            d="M 363.64795,350 V 246.80176 h 81.15234 v 16.69922 h -59.5459 v 25.92773 h 55.07813 v 16.69922 h -55.07813 v 27.17285 h 62.54883 V 350 Z m 200.39062,-52.0752 q 0,16.11329 -6.37207,28.34473 -6.37207,12.23145 -18.2373,18.75 -11.86524,6.44531 -27.68555,6.44531 -24.3164,0 -38.15918,-14.28222 -13.76953,-14.35547 -13.76953,-39.25782 0,-24.8291 13.76953,-38.74511 13.76953,-13.91602 38.30567,-13.91602 24.53613,0 38.30566,14.0625 13.84277,14.0625 13.84277,38.59863 z m -22.04589,0 q 0,-16.69921 -7.91016,-26.14746 -7.91016,-9.52148 -22.19238,-9.52148 -14.50196,0 -22.41211,9.44824 -7.91016,9.375 -7.91016,26.2207 0,16.99219 8.05664,26.80665 8.12988,9.74121 22.11914,9.74121 14.50196,0 22.33887,-9.52149 7.91016,-9.52148 7.91016,-27.02637 z m 122.53418,22.33887 q 0,15.16113 -11.2793,23.21778 -11.20606,7.98339 -32.95899,7.98339 -19.84863,0 -31.12793,-7.03125 -11.27929,-7.03125 -14.50195,-21.31347 l 20.87402,-3.44239 q 2.12403,8.20313 8.27637,11.93848 6.15235,3.66211 17.06543,3.66211 22.63184,0 22.63184,-13.76953 0,-4.39453 -2.63672,-7.25098 -2.56348,-2.85644 -7.32422,-4.76074 -4.6875,-1.9043 -18.09082,-4.61426 -11.57227,-2.70996 -16.11328,-4.32129 -4.54102,-1.68457 -8.20313,-3.88183 -3.66211,-2.27051 -6.22558,-5.41992 -2.56348,-3.14942 -4.02832,-7.39747 -1.3916,-4.24804 -1.3916,-9.74121 0,-13.98925 10.47363,-21.38672 10.54687,-7.4707 30.61523,-7.4707 19.18946,0 28.78418,6.00586 9.66797,6.00586 12.45117,19.84863 l -20.94726,2.85645 q -1.61133,-6.66504 -6.5918,-10.03418 -4.90722,-3.36914 -14.13574,-3.36914 -19.62891,0 -19.62891,12.30469 0,4.02832 2.05078,6.59179 2.12403,2.56348 6.22559,4.39453 4.10156,1.83106 16.62598,4.46778 14.86816,3.14941 21.24023,5.85937 6.44531,2.63672 10.18067,6.22559 3.73535,3.51562 5.71289,8.49609 1.97754,4.90723 1.97754,11.35254 z"
-            id="text2"
-            style="font-weight:700;font-size:150px;font-family:Arial, Helvetica, sans-serif;text-anchor:middle;fill:#f5f8ff"
-            aria-label="EOS"/>
+    <g transform="translate(512 512) scale(1.4) translate(-512 -512)">
+        <path
+                d="M 363.64795,350 V 246.80176 h 81.15234 v 16.69922 h -59.5459 v 25.92773 h 55.07813 v 16.69922 h -55.07813 v 27.17285 h 62.54883 V 350 Z m 200.39062,-52.0752 q 0,16.11329 -6.37207,28.34473 -6.37207,12.23145 -18.2373,18.75 -11.86524,6.44531 -27.68555,6.44531 -24.3164,0 -38.15918,-14.28222 -13.76953,-14.35547 -13.76953,-39.25782 0,-24.8291 13.76953,-38.74511 13.76953,-13.91602 38.30567,-13.91602 24.53613,0 38.30566,14.0625 13.84277,14.0625 13.84277,38.59863 z m -22.04589,0 q 0,-16.69921 -7.91016,-26.14746 -7.91016,-9.52148 -22.19238,-9.52148 -14.50196,0 -22.41211,9.44824 -7.91016,9.375 -7.91016,26.2207 0,16.99219 8.05664,26.80665 8.12988,9.74121 22.11914,9.74121 14.50196,0 22.33887,-9.52149 7.91016,-9.52148 7.91016,-27.02637 z m 122.53418,22.33887 q 0,15.16113 -11.2793,23.21778 -11.20606,7.98339 -32.95899,7.98339 -19.84863,0 -31.12793,-7.03125 -11.27929,-7.03125 -14.50195,-21.31347 l 20.87402,-3.44239 q 2.12403,8.20313 8.27637,11.93848 6.15235,3.66211 17.06543,3.66211 22.63184,0 22.63184,-13.76953 0,-4.39453 -2.63672,-7.25098 -2.56348,-2.85644 -7.32422,-4.76074 -4.6875,-1.9043 -18.09082,-4.61426 -11.57227,-2.70996 -16.11328,-4.32129 -4.54102,-1.68457 -8.20313,-3.88183 -3.66211,-2.27051 -6.22558,-5.41992 -2.56348,-3.14942 -4.02832,-7.39747 -1.3916,-4.24804 -1.3916,-9.74121 0,-13.98925 10.47363,-21.38672 10.54687,-7.4707 30.61523,-7.4707 19.18946,0 28.78418,6.00586 9.66797,6.00586 12.45117,19.84863 l -20.94726,2.85645 q -1.61133,-6.66504 -6.5918,-10.03418 -4.90722,-3.36914 -14.13574,-3.36914 -19.62891,0 -19.62891,12.30469 0,4.02832 2.05078,6.59179 2.12403,2.56348 6.22559,4.39453 4.10156,1.83106 16.62598,4.46778 14.86816,3.14941 21.24023,5.85937 6.44531,2.63672 10.18067,6.22559 3.73535,3.51562 5.71289,8.49609 1.97754,4.90723 1.97754,11.35254 z"
+                id="text2"
+                style="font-weight:700;font-size:150px;font-family:Arial, Helvetica, sans-serif;text-anchor:middle;fill:#f5f8ff"
+                aria-label="EOS"/>
 
-    <path
-            d="M300 450H610V365L792 513L610 661V575H300Z"
-            fill="#6fd2ff"
-            id="path2"/>
+        <path
+                d="M300 450H610V365L792 513L610 661V575H300Z"
+                fill="#6fd2ff"
+                id="path2"/>
 
-    <path
-            d="m 438.48828,730.50586 q 0,14.87109 -7.33008,24.78516 -7.27734,9.86132 -20.30273,12.49804 1.79297,5.22071 5.00976,7.54102 3.26954,2.32031 9.12305,2.32031 3.11133,0 6.27539,-0.52734 l -0.10547,10.65234 q -6.64453,1.47656 -12.76172,1.47656 -8.5957,0 -14.23828,-4.79883 -5.64258,-4.79882 -9.07031,-15.92578 -15.0293,-1.42382 -23.36133,-11.44336 -8.27929,-10.07226 -8.27929,-26.57812 0,-17.87695 9.91406,-27.89649 9.91406,-10.01953 27.58008,-10.01953 17.66601,0 27.58007,10.125 9.9668,10.125 9.9668,27.79102 z m -15.87305,0 q 0,-12.02344 -5.69531,-18.82617 -5.69531,-6.85547 -15.97851,-6.85547 -10.44141,0 -16.13672,6.80273 -5.69532,6.75 -5.69532,18.87891 0,12.23437 5.80079,19.30078 5.80078,7.06641 15.92578,7.06641 10.4414,0 16.08398,-6.85547 5.69531,-6.85547 5.69531,-19.51172 z M 450.24805,768 v -74.30273 h 15.55664 v 62.27929 h 39.86718 V 768 Z m 118.49414,0 -6.5918,-18.98438 H 533.83203 L 527.24023,768 h -15.55664 l 27.10547,-74.30273 h 18.35156 l 27,74.30273 z m -10.07227,-30.69141 -7.33008,-21.35742 -3.375,-10.81055 q -0.84375,3.0586 -1.58203,5.48438 -0.73828,2.42578 -9.07031,26.68359 z m 101.4082,9.49219 q 0,10.125 -7.59375,15.66211 Q 644.89062,768 631.39062,768 h -37.17773 v -74.30273 h 34.01367 q 13.60547,0 20.56641,4.74609 7.01367,4.69336 7.01367,13.92187 0,6.32813 -3.5332,10.70508 -3.48047,4.32422 -10.65235,5.85352 9.01758,1.05469 13.71094,5.69531 4.74609,4.58789 4.74609,12.18164 z m -15.60937,-1.21289 q 0,-10.44141 -14.97656,-10.44141 h -19.72266 v 21.30469 h 20.30274 q 7.48828,0 10.91601,-2.68945 3.48047,-2.74219 3.48047,-8.17383 z m -4.32422,-31.11328 q 0,-5.00977 -3.2168,-7.11914 -3.16406,-2.10938 -9.43945,-2.10938 h -17.71875 v 18.4043 h 17.82422 q 6.5918,0 9.54492,-2.26758 3.00586,-2.32031 3.00586,-6.9082 z"
-            id="text3"
-            style="font-weight:700;font-size:108px;font-family:Arial, Helvetica, sans-serif;text-anchor:middle;fill:#f5f8ff"
-            aria-label="QLAB"/>
-  </g>
+        <path
+                d="m 438.48828,730.50586 q 0,14.87109 -7.33008,24.78516 -7.27734,9.86132 -20.30273,12.49804 1.79297,5.22071 5.00976,7.54102 3.26954,2.32031 9.12305,2.32031 3.11133,0 6.27539,-0.52734 l -0.10547,10.65234 q -6.64453,1.47656 -12.76172,1.47656 -8.5957,0 -14.23828,-4.79883 -5.64258,-4.79882 -9.07031,-15.92578 -15.0293,-1.42382 -23.36133,-11.44336 -8.27929,-10.07226 -8.27929,-26.57812 0,-17.87695 9.91406,-27.89649 9.91406,-10.01953 27.58008,-10.01953 17.66601,0 27.58007,10.125 9.9668,10.125 9.9668,27.79102 z m -15.87305,0 q 0,-12.02344 -5.69531,-18.82617 -5.69531,-6.85547 -15.97851,-6.85547 -10.44141,0 -16.13672,6.80273 -5.69532,6.75 -5.69532,18.87891 0,12.23437 5.80079,19.30078 5.80078,7.06641 15.92578,7.06641 10.4414,0 16.08398,-6.85547 5.69531,-6.85547 5.69531,-19.51172 z M 450.24805,768 v -74.30273 h 15.55664 v 62.27929 h 39.86718 V 768 Z m 118.49414,0 -6.5918,-18.98438 H 533.83203 L 527.24023,768 h -15.55664 l 27.10547,-74.30273 h 18.35156 l 27,74.30273 z m -10.07227,-30.69141 -7.33008,-21.35742 -3.375,-10.81055 q -0.84375,3.0586 -1.58203,5.48438 -0.73828,2.42578 -9.07031,26.68359 z m 101.4082,9.49219 q 0,10.125 -7.59375,15.66211 Q 644.89062,768 631.39062,768 h -37.17773 v -74.30273 h 34.01367 q 13.60547,0 20.56641,4.74609 7.01367,4.69336 7.01367,13.92187 0,6.32813 -3.5332,10.70508 -3.48047,4.32422 -10.65235,5.85352 9.01758,1.05469 13.71094,5.69531 4.74609,4.58789 4.74609,12.18164 z m -15.60937,-1.21289 q 0,-10.44141 -14.97656,-10.44141 h -19.72266 v 21.30469 h 20.30274 q 7.48828,0 10.91601,-2.68945 3.48047,-2.74219 3.48047,-8.17383 z m -4.32422,-31.11328 q 0,-5.00977 -3.2168,-7.11914 -3.16406,-2.10938 -9.43945,-2.10938 h -17.71875 v 18.4043 h 17.82422 q 6.5918,0 9.54492,-2.26758 3.00586,-2.32031 3.00586,-6.9082 z"
+                id="text3"
+                style="font-weight:700;font-size:108px;font-family:Arial, Helvetica, sans-serif;text-anchor:middle;fill:#f5f8ff"
+                aria-label="QLAB"/>
+    </g>
 </svg>
\ No newline at end of file
diff --git a/src/EosToQLab.Core/Exceptions/ShowDataEntryMissingException.cs b/src/EosToQLab.Core/Exceptions/ShowDataEntryMissingException.cs
index 438d9d1..a21040b 100644
--- a/src/EosToQLab.Core/Exceptions/ShowDataEntryMissingException.cs
+++ b/src/EosToQLab.Core/Exceptions/ShowDataEntryMissingException.cs
@@ -3,7 +3,7 @@ namespace EosToQLab.Core.Exceptions;
 public sealed class ShowDataEntryMissingException : EosToQLabException
 {
     public ShowDataEntryMissingException(string fileName)
-        : base("EOS_ESF3D_SHOWDAT_MISSING", $"The ESF3D archive '{fileName}' does not contain showdat.dat.")
+        : base("EOS_SHOW_ARCHIVE_SHOWDAT_MISSING", $"The EOS show archive '{fileName}' does not contain showdat.dat.")
     {
     }
 }
\ No newline at end of file
diff --git a/src/EosToQLab.Core/Exceptions/ShowDataReadException.cs b/src/EosToQLab.Core/Exceptions/ShowDataReadException.cs
index 18bddc3..3d4c73f 100644
--- a/src/EosToQLab.Core/Exceptions/ShowDataReadException.cs
+++ b/src/EosToQLab.Core/Exceptions/ShowDataReadException.cs
@@ -3,7 +3,7 @@ namespace EosToQLab.Core.Exceptions;
 public sealed class ShowDataReadException : EosToQLabException
 {
     public ShowDataReadException(string fileName, Exception innerException)
-        : base("EOS_ESF3D_SHOWDAT_READ_FAILED", $"showdat.dat could not be read from '{fileName}'.", innerException)
+        : base("EOS_SHOW_ARCHIVE_SHOWDAT_READ_FAILED", $"showdat.dat could not be read from '{fileName}'.", innerException)
     {
     }
 }
\ No newline at end of file
diff --git a/src/EosToQLab.Core/Exceptions/UnsupportedEosImportFormatException.cs b/src/EosToQLab.Core/Exceptions/UnsupportedEosImportFormatException.cs
index c5b6555..ef26db0 100644
--- a/src/EosToQLab.Core/Exceptions/UnsupportedEosImportFormatException.cs
+++ b/src/EosToQLab.Core/Exceptions/UnsupportedEosImportFormatException.cs
@@ -3,7 +3,7 @@ namespace EosToQLab.Core.Exceptions;
 public sealed class UnsupportedEosImportFormatException : EosToQLabException
 {
     public UnsupportedEosImportFormatException(string fileName)
-        : base("EOS_IMPORT_FORMAT_UNSUPPORTED", $"The file '{fileName}' is not a supported EOS CSV or ESF3D source.")
+        : base("EOS_IMPORT_FORMAT_UNSUPPORTED", $"The file '{fileName}' is not a supported EOS CSV, ESF2, or ESF3D source.")
     {
     }
 }
\ No newline at end of file
diff --git a/src/EosToQLab.Core/Models/EosSourceKind.cs b/src/EosToQLab.Core/Models/EosSourceKind.cs
index b7fc100..3fa4ab7 100644
--- a/src/EosToQLab.Core/Models/EosSourceKind.cs
+++ b/src/EosToQLab.Core/Models/EosSourceKind.cs
@@ -3,5 +3,5 @@ namespace EosToQLab.Core.Models;
 public enum EosSourceKind
 {
     Csv,
-    Esf3d
+    ShowArchive
 }
\ No newline at end of file
diff --git a/src/EosToQLab.Infrastructure/EosToQLab.Infrastructure.csproj b/src/EosToQLab.Infrastructure/EosToQLab.Infrastructure.csproj
index 160e94f..9c5ac9b 100644
--- a/src/EosToQLab.Infrastructure/EosToQLab.Infrastructure.csproj
+++ b/src/EosToQLab.Infrastructure/EosToQLab.Infrastructure.csproj
@@ -2,8 +2,26 @@
     <PropertyGroup>
         <TargetFramework>net10.0</TargetFramework>
         <RootNamespace>EosToQLab.Infrastructure</RootNamespace>
+        <EosShowArchiveCompatibilityFixtureRoot>$([System.IO.Path]::GetFullPath('$(MSBuildThisFileDirectory)../../tests/EosToQLab.Tests/Fixtures/EosShowArchive/Compatibility'))</EosShowArchiveCompatibilityFixtureRoot>
+        <EosShowArchiveCompatibilityCatalog>$(IntermediateOutputPath)eos-show-archive-tested-archives.json</EosShowArchiveCompatibilityCatalog>
+        <EosShowArchiveCompatibilityGenerator>$([System.IO.Path]::GetFullPath('$(MSBuildThisFileDirectory)../../tools/EosToQLab.CompatibilityCatalogGenerator/bin/$(Configuration)/net10.0/EosToQLab.CompatibilityCatalogGenerator.dll'))</EosShowArchiveCompatibilityGenerator>
     </PropertyGroup>
     <ItemGroup>
         <ProjectReference Include="../EosToQLab.Core/EosToQLab.Core.csproj"/>
+        <ProjectReference Include="../../tools/EosToQLab.CompatibilityCatalogGenerator/EosToQLab.CompatibilityCatalogGenerator.csproj"
+                          ReferenceOutputAssembly="false" />
+        <_EosShowArchiveCompatibilityFixture Include="$(EosShowArchiveCompatibilityFixtureRoot)/**/*" />
+        <EmbeddedResource Include="$(EosShowArchiveCompatibilityCatalog)"
+                          Condition="'$(DesignTimeBuild)' != 'true'"
+                          LogicalName="EosToQLab.Infrastructure.Import.EosShowArchive.tested-archives.json" />
     </ItemGroup>
+    <Target Name="GenerateEosShowArchiveCompatibilityCatalog"
+            BeforeTargets="PrepareResourceNames"
+            DependsOnTargets="ResolveProjectReferences"
+            Inputs="@(_EosShowArchiveCompatibilityFixture);$(EosShowArchiveCompatibilityGenerator)"
+            Outputs="$(EosShowArchiveCompatibilityCatalog)"
+            Condition="'$(DesignTimeBuild)' != 'true'">
+        <MakeDir Directories="$(IntermediateOutputPath)" />
+        <Exec Command="dotnet &quot;$(EosShowArchiveCompatibilityGenerator)&quot; &quot;$(EosShowArchiveCompatibilityFixtureRoot)&quot; &quot;$(EosShowArchiveCompatibilityCatalog)&quot;" />
+    </Target>
 </Project>
diff --git a/tests/EosToQLab.Tests/.editorconfig b/tests/EosToQLab.Tests/.editorconfig
index 12e4584..ef278dc 100644
--- a/tests/EosToQLab.Tests/.editorconfig
+++ b/tests/EosToQLab.Tests/.editorconfig
@@ -1,2 +1,3 @@
 [*.cs]
-resharper_inconsistent_naming_highlighting = none
\ No newline at end of file
+resharper_inconsistent_naming_highlighting = none
+dotnet_diagnostic.CA1707.severity = none
\ No newline at end of file
diff --git a/tests/EosToQLab.Tests/Core/Exceptions/ShowDataEntryMissingExceptionTests.cs b/tests/EosToQLab.Tests/Core/Exceptions/ShowDataEntryMissingExceptionTests.cs
index 98a803d..e9a7f23 100644
--- a/tests/EosToQLab.Tests/Core/Exceptions/ShowDataEntryMissingExceptionTests.cs
+++ b/tests/EosToQLab.Tests/Core/Exceptions/ShowDataEntryMissingExceptionTests.cs
@@ -9,6 +9,6 @@ public sealed class ShowDataEntryMissingExceptionTests
     {
         var exception = new ShowDataEntryMissingException("show.esf3d");
 
-        ExceptionAssertions.HasDetails(exception, "EOS_ESF3D_SHOWDAT_MISSING", "show.esf3d");
+        ExceptionAssertions.HasDetails(exception, "EOS_SHOW_ARCHIVE_SHOWDAT_MISSING", "show.esf3d");
     }
 }
\ No newline at end of file
diff --git a/tests/EosToQLab.Tests/Core/Exceptions/ShowDataReadExceptionTests.cs b/tests/EosToQLab.Tests/Core/Exceptions/ShowDataReadExceptionTests.cs
index 3224d5d..7135318 100644
--- a/tests/EosToQLab.Tests/Core/Exceptions/ShowDataReadExceptionTests.cs
+++ b/tests/EosToQLab.Tests/Core/Exceptions/ShowDataReadExceptionTests.cs
@@ -10,7 +10,7 @@ public sealed class ShowDataReadExceptionTests
         var inner = new InvalidOperationException("inner");
         var exception = new ShowDataReadException("show.esf3d", inner);
 
-        ExceptionAssertions.HasDetails(exception, "EOS_ESF3D_SHOWDAT_READ_FAILED", "show.esf3d");
+        ExceptionAssertions.HasDetails(exception, "EOS_SHOW_ARCHIVE_SHOWDAT_READ_FAILED", "show.esf3d");
         Assert.Same(inner, exception.InnerException);
     }
 }
\ No newline at end of file
diff --git a/tests/EosToQLab.Tests/Core/Import/EosShowArchiveCompatibilityTests.cs b/tests/EosToQLab.Tests/Core/Import/EosShowArchiveCompatibilityTests.cs
index c973c2a..cefd8cd 100644
--- a/tests/EosToQLab.Tests/Core/Import/EosShowArchiveCompatibilityTests.cs
+++ b/tests/EosToQLab.Tests/Core/Import/EosShowArchiveCompatibilityTests.cs
@@ -30,12 +30,12 @@ public sealed class EosShowArchiveCompatibilityTests
                      .Where(path => IsShowArchiveExtension(Path.GetExtension(path))))
         {
             await using var stream = File.OpenRead(archivePath);
-            using var archive = new ZipArchive(stream, ZipArchiveMode.Read);
+            await using var archive = new ZipArchive(stream, ZipArchiveMode.Read);
             var versionEntry = archive.Entries.FirstOrDefault(entry =>
                 entry.FullName.EndsWith("version.json", StringComparison.OrdinalIgnoreCase));
             if (versionEntry is null) continue;
 
-            await using var input = versionEntry.Open();
+            await using var input = await versionEntry.OpenAsync(TestContext.Current.CancellationToken);
             using var document = await JsonDocument.ParseAsync(input, cancellationToken: TestContext.Current.CancellationToken);
             manifests.Add((
                 document.RootElement.GetProperty("Format").GetString()!,
diff --git a/tests/EosToQLab.Tests/Core/Models/EosSourceKindTests.cs b/tests/EosToQLab.Tests/Core/Models/EosSourceKindTests.cs
index 40f9b7b..b2718a1 100644
--- a/tests/EosToQLab.Tests/Core/Models/EosSourceKindTests.cs
+++ b/tests/EosToQLab.Tests/Core/Models/EosSourceKindTests.cs
@@ -3,8 +3,8 @@ namespace EosToQLab.Tests.Core.Models;
 public sealed class EosSourceKindTests
 {
     [Fact]
-    public void Defines_expected_values()
+    public void Defines_csv_and_show_archive_sources()
     {
-        Assert.NotEmpty(Enum.GetValues<EosSourceKind>());
+        Assert.Equal([EosSourceKind.Csv, EosSourceKind.ShowArchive], Enum.GetValues<EosSourceKind>());
     }
-}
\ No newline at end of file
+}
diff --git a/tests/EosToQLab.Tests/EosToQLab.Tests.csproj b/tests/EosToQLab.Tests/EosToQLab.Tests.csproj
index 523f34e..86cab8f 100644
--- a/tests/EosToQLab.Tests/EosToQLab.Tests.csproj
+++ b/tests/EosToQLab.Tests/EosToQLab.Tests.csproj
@@ -1,41 +1,41 @@
 <Project Sdk="Microsoft.NET.Sdk">
-    <PropertyGroup>
-        <TargetFramework>net10.0</TargetFramework>
-        <IsPackable>false</IsPackable>
-        <OutputType>Exe</OutputType>
-        <IsTestProject>true</IsTestProject>
-        <RootNamespace>EosToQLab.Tests</RootNamespace>
-        <CollectCoverage>true</CollectCoverage>
-        <CoverletOutputFormat>cobertura</CoverletOutputFormat>
-        <Threshold>100</Threshold>
-        <ThresholdType>line</ThresholdType>
-        <ThresholdStat>total</ThresholdStat>
-        <Include>[EosToQLab.Core]*,[EosToQLab.Infrastructure]*</Include>
-    </PropertyGroup>
-    <ItemGroup>
-        <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.14.1"/>
-        <PackageReference Include="xunit.v3" Version="3.2.2"/>
-        <PackageReference Include="xunit.runner.visualstudio" Version="3.1.5">
-            <PrivateAssets>all</PrivateAssets>
-            <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
-        </PackageReference>
-        <PackageReference Include="coverlet.msbuild" Version="6.0.4">
-            <PrivateAssets>all</PrivateAssets>
-            <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
-        </PackageReference>
-    </ItemGroup>
-    <ItemGroup>
-        <ProjectReference Include="../../src/EosToQLab.Core/EosToQLab.Core.csproj"/>
-        <ProjectReference Include="../../src/EosToQLab.Infrastructure/EosToQLab.Infrastructure.csproj"/>
-    </ItemGroup>
-    <ItemGroup>
-        <None Include="../../samples/reference-eos.csv" Link="Fixtures/Csv/reference-eos.csv" CopyToOutputDirectory="PreserveNewest"/>
-        <None Update="Fixtures/Esf3d/**/*.esf3d" CopyToOutputDirectory="PreserveNewest"/>
-        <None Update="Fixtures/Esf3d/**/*.json" CopyToOutputDirectory="PreserveNewest"/>
-        <None Update="Fixtures/Esf3d/**/*.md" CopyToOutputDirectory="PreserveNewest"/>
-    </ItemGroup>
-
-    <PropertyGroup>
-        <NoWarn>$(NoWarn);CA1707</NoWarn>
-    </PropertyGroup>
+  <PropertyGroup>
+    <TargetFramework>net10.0</TargetFramework>
+    <IsPackable>false</IsPackable>
+    <OutputType>Exe</OutputType>
+    <IsTestProject>true</IsTestProject>
+    <RootNamespace>EosToQLab.Tests</RootNamespace>
+  </PropertyGroup>
+  <ItemGroup>
+    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.14.1" />
+    <PackageReference Include="xunit.v3" Version="3.2.2" />
+    <PackageReference Include="xunit.runner.visualstudio" Version="3.1.5">
+      <PrivateAssets>all</PrivateAssets>
+      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
+    </PackageReference>
+    <PackageReference Include="coverlet.msbuild" Version="6.0.4">
+      <PrivateAssets>all</PrivateAssets>
+      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
+    </PackageReference>
+  </ItemGroup>
+  <ItemGroup>
+    <ProjectReference Include="../../src/EosToQLab.Core/EosToQLab.Core.csproj" />
+    <ProjectReference Include="../../src/EosToQLab.Infrastructure/EosToQLab.Infrastructure.csproj" />
+  </ItemGroup>
+  <ItemGroup>
+    <None Include="../../samples/reference-eos.csv" Link="Fixtures/Csv/reference-eos.csv" CopyToOutputDirectory="PreserveNewest" />
+    <None Update="Fixtures/EosShowArchive/**/*.esf3d" CopyToOutputDirectory="PreserveNewest" />
+    <None Update="Fixtures/EosShowArchive/**/*.esf2" CopyToOutputDirectory="PreserveNewest" />
+    <None Update="Fixtures/EosShowArchive/**/*.json" CopyToOutputDirectory="PreserveNewest" />
+    <None Update="Fixtures/EosShowArchive/**/*.md" CopyToOutputDirectory="PreserveNewest" />
+    <None Update="Fixtures\Esf3d\expected.json">
+      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
+    </None>
+    <None Update="Fixtures\EosShowArchive\Regression\dense-cue-records\expected.json">
+      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
+    </None>
+    <None Update="Fixtures\EosShowArchive\Compatibility\test\expected.json">
+      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
+    </None>
+  </ItemGroup>
 </Project>
diff --git a/tests/EosToQLab.Tests/Fixtures/EosShowArchive/Compatibility/eos-3.3.5.69/expected.json b/tests/EosToQLab.Tests/Fixtures/EosShowArchive/Compatibility/eos-3.3.5.69/expected.json
index 975ec20..590e781 100644
--- a/tests/EosToQLab.Tests/Fixtures/EosShowArchive/Compatibility/eos-3.3.5.69/expected.json
+++ b/tests/EosToQLab.Tests/Fixtures/EosShowArchive/Compatibility/eos-3.3.5.69/expected.json
@@ -36,7 +36,7 @@
       "sourceOrder": 4,
       "listNumber": 1,
       "cueNumber": "5",
-      "label": "Only Lable"
+      "label": "Only Label"
     },
     {
       "sourceOrder": 5,
diff --git a/tests/EosToQLab.Tests/Fixtures/EosShowArchive/Regression/real-cue-901-labelled/expected.json b/tests/EosToQLab.Tests/Fixtures/EosShowArchive/Regression/real-cue-901-labelled/expected.json
index eae9a23..a3213aa 100644
--- a/tests/EosToQLab.Tests/Fixtures/EosShowArchive/Regression/real-cue-901-labelled/expected.json
+++ b/tests/EosToQLab.Tests/Fixtures/EosShowArchive/Regression/real-cue-901-labelled/expected.json
@@ -36,7 +36,7 @@
       "sourceOrder": 4,
       "listNumber": 1,
       "cueNumber": "5",
-      "label": "Only Lable"
+      "label": "Only Label"
     },
     {
       "sourceOrder": 5,
@@ -54,7 +54,7 @@
       "sourceOrder": 7,
       "listNumber": 1,
       "cueNumber": "901",
-      "label": "test"
+      "label": "With Label"
     }
   ]
 }
diff --git a/tests/EosToQLab.Tests/Fixtures/EosShowArchive/Regression/real-cue-901/expected.json b/tests/EosToQLab.Tests/Fixtures/EosShowArchive/Regression/real-cue-901/expected.json
index 580423d..e541aa1 100644
--- a/tests/EosToQLab.Tests/Fixtures/EosShowArchive/Regression/real-cue-901/expected.json
+++ b/tests/EosToQLab.Tests/Fixtures/EosShowArchive/Regression/real-cue-901/expected.json
@@ -36,7 +36,7 @@
       "sourceOrder": 4,
       "listNumber": 1,
       "cueNumber": "5",
-      "label": "Only Lable"
+      "label": "Only Label"
     },
     {
       "sourceOrder": 5,
diff --git a/tests/EosToQLab.Tests/Fixtures/Esf3d/expected.json b/tests/EosToQLab.Tests/Fixtures/Esf3d/expected.json
index 590e781..ddf11f7 100644
--- a/tests/EosToQLab.Tests/Fixtures/Esf3d/expected.json
+++ b/tests/EosToQLab.Tests/Fixtures/Esf3d/expected.json
@@ -3,7 +3,7 @@
     "format": "{CB82CC14-5598-4DB1-A1D7-EBC3BE1D1038}",
     "version": "3.3.5.69"
   },
-  "fixtureVersion": "synthetic-known-3.3.5_Build_69",
+  "fixtureVersion": "Every_declared_tested_version_has_a_versioned_golden_fixture",
   "purpose": "Stable parser contract independent of an installed ETC Eos version.",
   "cues": [
     {
diff --git a/tests/EosToQLab.Tests/GlobalUsings.cs b/tests/EosToQLab.Tests/GlobalUsings.cs
index fdeb411..4247792 100644
--- a/tests/EosToQLab.Tests/GlobalUsings.cs
+++ b/tests/EosToQLab.Tests/GlobalUsings.cs
@@ -8,7 +8,7 @@ global using EosToQLab.Core.QLab;
 global using EosToQLab.Core.Security;
 global using EosToQLab.Infrastructure.Import;
 global using EosToQLab.Infrastructure.Import.Csv;
-global using EosToQLab.Infrastructure.Import.Esf3d;
+global using EosToQLab.Infrastructure.Import.EosShowArchive;
 global using EosToQLab.Infrastructure.QLab;
 global using EosToQLab.Infrastructure.QLab.Osc;
 global using EosToQLab.Infrastructure.QLab.Workflow;
\ No newline at end of file
diff --git a/tests/EosToQLab.Tests/Infrastructure/Import/EosCueImporterFactoryTests.cs b/tests/EosToQLab.Tests/Infrastructure/Import/EosCueImporterFactoryTests.cs
index 3f1b4c7..c48a464 100644
--- a/tests/EosToQLab.Tests/Infrastructure/Import/EosCueImporterFactoryTests.cs
+++ b/tests/EosToQLab.Tests/Infrastructure/Import/EosCueImporterFactoryTests.cs
@@ -7,18 +7,16 @@ public sealed class EosCueImporterFactoryTests
     [Fact]
     public void Selects_first_matching_importer()
     {
-        var first = new FakeEosCueImporter(EosSourceKind.Csv,
-            name => name.EndsWith(".csv", StringComparison.OrdinalIgnoreCase));
-        var second = new FakeEosCueImporter(EosSourceKind.Esf3d, _ => true);
+        var first = new FakeEosCueImporter(EosSourceKind.Csv, name => name.EndsWith(".csv", StringComparison.OrdinalIgnoreCase));
+        var second = new FakeEosCueImporter(EosSourceKind.ShowArchive, _ => true);
         var factory = new EosCueImporterFactory([first, second]);
 
         Assert.Same(first, factory.CreateFor("show.csv"));
         Assert.Same(second, factory.CreateFor("show.esf3d"));
+        Assert.Same(second, factory.CreateFor("show.esf2"));
     }
 
     [Fact]
-    public void Throws_for_unsupported_file()
-    {
+    public void Throws_for_unsupported_file() =>
         Assert.Throws<UnsupportedEosImportFormatException>(() => new EosCueImporterFactory([]).CreateFor("show.txt"));
-    }
-}
\ No newline at end of file
+}
diff --git a/tests/EosToQLab.Tests/Infrastructure/Import/EosShowArchive/EmbeddedEosShowArchiveCompatibilityTests.cs b/tests/EosToQLab.Tests/Infrastructure/Import/EosShowArchive/EmbeddedEosShowArchiveCompatibilityTests.cs
index 218de8f..3cb48ce 100644
--- a/tests/EosToQLab.Tests/Infrastructure/Import/EosShowArchive/EmbeddedEosShowArchiveCompatibilityTests.cs
+++ b/tests/EosToQLab.Tests/Infrastructure/Import/EosShowArchive/EmbeddedEosShowArchiveCompatibilityTests.cs
@@ -33,7 +33,7 @@ public sealed class EmbeddedEosShowArchiveCompatibilityTests
         var expected = Directory.EnumerateDirectories(fixtureRoot)
             .Select(directory => Path.Combine(directory, "expected.json"))
             .Select(File.ReadAllText)
-            .Select(JsonDocument.Parse)
+            .Select(text => JsonDocument.Parse(text))
             .Select(document =>
             {
                 using (document)
diff --git a/tests/EosToQLab.Tests/Infrastructure/Import/EosShowArchive/EosShowArchiveCueImporterTests.cs b/tests/EosToQLab.Tests/Infrastructure/Import/EosShowArchive/EosShowArchiveCueImporterTests.cs
index e9fb927..ffabf77 100644
--- a/tests/EosToQLab.Tests/Infrastructure/Import/EosShowArchive/EosShowArchiveCueImporterTests.cs
+++ b/tests/EosToQLab.Tests/Infrastructure/Import/EosShowArchive/EosShowArchiveCueImporterTests.cs
@@ -190,7 +190,7 @@ public sealed class EosShowArchiveCueImporterTests
 
     [Theory]
     [InlineData("real-cue-901", null)]
-    [InlineData("real-cue-901-labelled", "test")]
+    [InlineData("real-cue-901-labelled", "With Label")]
     public async Task Keeps_a_real_cue_901_and_does_not_attach_global_effect_text(
         string fixtureDirectory,
         string? expectedLabel)
@@ -215,7 +215,7 @@ public sealed class EosShowArchiveCueImporterTests
             "EosShowArchive",
             "Regression",
             "dense-cue-records",
-            "dense-cue-record-variants-3.3.5.esf3d");
+            "dense-cue-record-variants-3.3.5.69.esf3d");
         await using var stream = File.OpenRead(fixture);
 
         var result = await _sut.ImportAsync(

=== STAGED CHANGES ===
diff --git a/src/EosToQLab.Core/Diagnostics/Esf3dFollowNotDecodedWarning.cs b/src/EosToQLab.Core/Diagnostics/EosShowArchiveFollowNotDecodedWarning.cs
similarity index 54%
rename from src/EosToQLab.Core/Diagnostics/Esf3dFollowNotDecodedWarning.cs
rename to src/EosToQLab.Core/Diagnostics/EosShowArchiveFollowNotDecodedWarning.cs
index 008bca9..8638f03 100644
--- a/src/EosToQLab.Core/Diagnostics/Esf3dFollowNotDecodedWarning.cs
+++ b/src/EosToQLab.Core/Diagnostics/EosShowArchiveFollowNotDecodedWarning.cs
@@ -1,8 +1,8 @@
 namespace EosToQLab.Core.Diagnostics;
 
-public sealed record Esf3dFollowNotDecodedWarning(string CueNumber) : EosWarning
+public sealed record EosShowArchiveFollowNotDecodedWarning(string CueNumber) : EosWarning
 {
-    public override string Code => "EOS_ESF3D_FOLLOW_NOT_DECODED";
+    public override string Code => "EOS_SHOW_ARCHIVE_FOLLOW_NOT_DECODED";
 
     public override string Message =>
         $"The follow/hang value of EOS cue {CueNumber} uses an unsupported showdat.dat encoding and was left empty.";
diff --git a/src/EosToQLab.Core/Diagnostics/EosShowArchiveLossTolerantParsingWarning.cs b/src/EosToQLab.Core/Diagnostics/EosShowArchiveLossTolerantParsingWarning.cs
new file mode 100644
index 0000000..8c6046f
--- /dev/null
+++ b/src/EosToQLab.Core/Diagnostics/EosShowArchiveLossTolerantParsingWarning.cs
@@ -0,0 +1,9 @@
+namespace EosToQLab.Core.Diagnostics;
+
+public sealed record EosShowArchiveLossTolerantParsingWarning : EosWarning
+{
+    public override string Code => "EOS_SHOW_ARCHIVE_LOSS_TOLERANT_PARSE";
+
+    public override string Message =>
+        "EOS show archive parsing is loss-tolerant because the proprietary showdat.dat object schema is not fully documented.";
+}
\ No newline at end of file
diff --git a/src/EosToQLab.Core/Diagnostics/Esf3dNoCueSequenceWarning.cs b/src/EosToQLab.Core/Diagnostics/EosShowArchiveNoCueSequenceWarning.cs
similarity index 50%
rename from src/EosToQLab.Core/Diagnostics/Esf3dNoCueSequenceWarning.cs
rename to src/EosToQLab.Core/Diagnostics/EosShowArchiveNoCueSequenceWarning.cs
index e68d71c..a38e3a7 100644
--- a/src/EosToQLab.Core/Diagnostics/Esf3dNoCueSequenceWarning.cs
+++ b/src/EosToQLab.Core/Diagnostics/EosShowArchiveNoCueSequenceWarning.cs
@@ -1,7 +1,7 @@
 namespace EosToQLab.Core.Diagnostics;
 
-public sealed record Esf3dNoCueSequenceWarning : EosWarning
+public sealed record EosShowArchiveNoCueSequenceWarning : EosWarning
 {
-    public override string Code => "EOS_ESF3D_NO_CUE_SEQUENCE";
+    public override string Code => "EOS_SHOW_ARCHIVE_NO_CUE_SEQUENCE";
     public override string Message => "No plausible monotonic cue sequence was found in showdat.dat.";
 }
\ No newline at end of file
diff --git a/src/EosToQLab.Core/Diagnostics/EosShowArchiveVersionNotTestedWarning.cs b/src/EosToQLab.Core/Diagnostics/EosShowArchiveVersionNotTestedWarning.cs
new file mode 100644
index 0000000..b49b79f
--- /dev/null
+++ b/src/EosToQLab.Core/Diagnostics/EosShowArchiveVersionNotTestedWarning.cs
@@ -0,0 +1,10 @@
+namespace EosToQLab.Core.Diagnostics;
+
+public sealed record EosShowArchiveVersionNotTestedWarning(string? Format, string? Version) : EosWarning
+{
+    public override string Code => "EOS_SHOW_ARCHIVE_VERSION_NOT_TESTED";
+
+    public override string Message => string.IsNullOrWhiteSpace(Format) || string.IsNullOrWhiteSpace(Version)
+        ? "The EOS show archive has no readable version.json. This archive version has not been verified by the bundled parser tests and may be incompatible."
+        : $"EOS show archive format '{Format}' version '{Version}' has not been verified by the bundled parser tests and may be incompatible.";
+}
diff --git a/src/EosToQLab.Core/Diagnostics/Esf3dLossTolerantParsingWarning.cs b/src/EosToQLab.Core/Diagnostics/Esf3dLossTolerantParsingWarning.cs
deleted file mode 100644
index c31519e..0000000
--- a/src/EosToQLab.Core/Diagnostics/Esf3dLossTolerantParsingWarning.cs
+++ /dev/null
@@ -1,9 +0,0 @@
-namespace EosToQLab.Core.Diagnostics;
-
-public sealed record Esf3dLossTolerantParsingWarning : EosWarning
-{
-    public override string Code => "EOS_ESF3D_LOSS_TOLERANT_PARSE";
-
-    public override string Message =>
-        "ESF3D parsing is loss-tolerant because the proprietary showdat.dat object schema is not fully documented.";
-}
\ No newline at end of file
diff --git a/src/EosToQLab.Core/Exceptions/EosShowArchiveInvalidException.cs b/src/EosToQLab.Core/Exceptions/EosShowArchiveInvalidException.cs
new file mode 100644
index 0000000..e776bab
--- /dev/null
+++ b/src/EosToQLab.Core/Exceptions/EosShowArchiveInvalidException.cs
@@ -0,0 +1,9 @@
+namespace EosToQLab.Core.Exceptions;
+
+public sealed class EosShowArchiveInvalidException : EosToQLabException
+{
+    public EosShowArchiveInvalidException(string fileName, Exception innerException)
+        : base("EOS_SHOW_ARCHIVE_INVALID", $"The EOS show archive '{fileName}' is not a valid ZIP archive.", innerException)
+    {
+    }
+}
\ No newline at end of file
diff --git a/src/EosToQLab.Core/Exceptions/Esf3dArchiveInvalidException.cs b/src/EosToQLab.Core/Exceptions/Esf3dArchiveInvalidException.cs
deleted file mode 100644
index e5e5268..0000000
--- a/src/EosToQLab.Core/Exceptions/Esf3dArchiveInvalidException.cs
+++ /dev/null
@@ -1,9 +0,0 @@
-namespace EosToQLab.Core.Exceptions;
-
-public sealed class Esf3dArchiveInvalidException : EosToQLabException
-{
-    public Esf3dArchiveInvalidException(string fileName, Exception innerException)
-        : base("EOS_ESF3D_ARCHIVE_INVALID", $"The ESF3D file '{fileName}' is not a valid ZIP archive.", innerException)
-    {
-    }
-}
\ No newline at end of file
diff --git a/src/EosToQLab.Core/Import/EosShowArchiveCompatibility.cs b/src/EosToQLab.Core/Import/EosShowArchiveCompatibility.cs
new file mode 100644
index 0000000..13ae537
--- /dev/null
+++ b/src/EosToQLab.Core/Import/EosShowArchiveCompatibility.cs
@@ -0,0 +1,20 @@
+namespace EosToQLab.Core.Import;
+
+public static class EosShowArchiveCompatibility
+{
+    public const string TestedFormat = "{CB82CC14-5598-4DB1-A1D7-EBC3BE1D1038}";
+
+    public static IReadOnlyList<(string Format, string Version)> TestedArchives { get; } =
+    [
+        (TestedFormat, "3.3.5.69")
+    ];
+
+    public static bool IsTested(string? format, string? version)
+    {
+        var normalizedFormat = format?.Trim();
+        var normalizedVersion = version?.Trim();
+        return TestedArchives.Any(candidate =>
+            string.Equals(candidate.Format, normalizedFormat, StringComparison.OrdinalIgnoreCase)
+            && string.Equals(candidate.Version, normalizedVersion, StringComparison.OrdinalIgnoreCase));
+    }
+}
diff --git a/src/EosToQLab.Core/Import/IEosShowArchiveCompatibility.cs b/src/EosToQLab.Core/Import/IEosShowArchiveCompatibility.cs
new file mode 100644
index 0000000..29d064a
--- /dev/null
+++ b/src/EosToQLab.Core/Import/IEosShowArchiveCompatibility.cs
@@ -0,0 +1,6 @@
+namespace EosToQLab.Core.Import;
+
+public interface IEosShowArchiveCompatibility
+{
+    bool IsCovered(string? format, string? version);
+}
diff --git a/src/EosToQLab.Infrastructure/Import/EosShowArchive/EmbeddedEosShowArchiveCompatibility.cs b/src/EosToQLab.Infrastructure/Import/EosShowArchive/EmbeddedEosShowArchiveCompatibility.cs
new file mode 100644
index 0000000..287bd76
--- /dev/null
+++ b/src/EosToQLab.Infrastructure/Import/EosShowArchive/EmbeddedEosShowArchiveCompatibility.cs
@@ -0,0 +1,111 @@
+using System.Reflection;
+using System.Text.Json;
+using EosToQLab.Core.Import;
+
+namespace EosToQLab.Infrastructure.Import.EosShowArchive;
+
+public sealed class EmbeddedEosShowArchiveCompatibility : IEosShowArchiveCompatibility
+{
+    internal const string ResourceName =
+        "EosToQLab.Infrastructure.Import.EosShowArchive.tested-archives.json";
+
+    private readonly HashSet<ArchiveIdentity> _coveredArchives;
+
+    public EmbeddedEosShowArchiveCompatibility()
+        : this(typeof(EmbeddedEosShowArchiveCompatibility).Assembly)
+    {
+    }
+
+    internal EmbeddedEosShowArchiveCompatibility(Assembly assembly)
+        : this(OpenEmbeddedCatalog(assembly))
+    {
+    }
+
+    internal EmbeddedEosShowArchiveCompatibility(Stream catalog)
+    {
+        ArgumentNullException.ThrowIfNull(catalog);
+
+        using (catalog)
+        using (var document = JsonDocument.Parse(catalog))
+        {
+            var root = document.RootElement;
+            if (root.ValueKind != JsonValueKind.Object
+                || !root.TryGetProperty("schemaVersion", out var schemaVersion)
+                || schemaVersion.ValueKind != JsonValueKind.Number
+                || schemaVersion.GetInt32() != 1
+                || !root.TryGetProperty("archives", out var archives)
+                || archives.ValueKind != JsonValueKind.Array)
+            {
+                throw new InvalidDataException(
+                    "The embedded EOS show archive compatibility catalog has an unsupported schema.");
+            }
+
+            _coveredArchives = archives
+                .EnumerateArray()
+                .Select(ParseIdentity)
+                .ToHashSet(ArchiveIdentityComparer.Instance);
+        }
+    }
+
+    public bool IsCovered(string? format, string? version)
+    {
+        if (string.IsNullOrWhiteSpace(format) || string.IsNullOrWhiteSpace(version)) return false;
+
+        return _coveredArchives.Contains(new ArchiveIdentity(format.Trim(), version.Trim()));
+    }
+
+    private static Stream OpenEmbeddedCatalog(Assembly assembly)
+    {
+        ArgumentNullException.ThrowIfNull(assembly);
+
+        return assembly.GetManifestResourceStream(ResourceName)
+               ?? throw new InvalidOperationException(
+                   $"The embedded EOS show archive compatibility catalog '{ResourceName}' is missing.");
+    }
+
+    private static ArchiveIdentity ParseIdentity(JsonElement element)
+    {
+        if (element.ValueKind != JsonValueKind.Object
+            || !element.TryGetProperty("format", out var formatProperty)
+            || formatProperty.ValueKind != JsonValueKind.String
+            || !element.TryGetProperty("version", out var versionProperty)
+            || versionProperty.ValueKind != JsonValueKind.String)
+        {
+            throw new InvalidDataException(
+                "The embedded EOS show archive compatibility catalog contains an invalid archive entry.");
+        }
+
+        var format = formatProperty.GetString()?.Trim();
+        var version = versionProperty.GetString()?.Trim();
+        if (string.IsNullOrWhiteSpace(format) || string.IsNullOrWhiteSpace(version))
+        {
+            throw new InvalidDataException(
+                "The embedded EOS show archive compatibility catalog contains an empty format or version.");
+        }
+
+        return new ArchiveIdentity(format, version);
+    }
+
+    private sealed record ArchiveIdentity(string Format, string Version);
+
+    private sealed class ArchiveIdentityComparer : IEqualityComparer<ArchiveIdentity>
+    {
+        public static ArchiveIdentityComparer Instance { get; } = new();
+
+        public bool Equals(ArchiveIdentity? x, ArchiveIdentity? y)
+        {
+            return ReferenceEquals(x, y)
+                   || x is not null
+                   && y is not null
+                   && StringComparer.OrdinalIgnoreCase.Equals(x.Format, y.Format)
+                   && StringComparer.OrdinalIgnoreCase.Equals(x.Version, y.Version);
+        }
+
+        public int GetHashCode(ArchiveIdentity obj)
+        {
+            return HashCode.Combine(
+                StringComparer.OrdinalIgnoreCase.GetHashCode(obj.Format),
+                StringComparer.OrdinalIgnoreCase.GetHashCode(obj.Version));
+        }
+    }
+}
diff --git a/src/EosToQLab.Infrastructure/Import/Esf3d/Esf3dCueHeaderDecoder.cs b/src/EosToQLab.Infrastructure/Import/EosShowArchive/EosShowArchiveCueHeaderDecoder.cs
similarity index 92%
rename from src/EosToQLab.Infrastructure/Import/Esf3d/Esf3dCueHeaderDecoder.cs
rename to src/EosToQLab.Infrastructure/Import/EosShowArchive/EosShowArchiveCueHeaderDecoder.cs
index f7461ce..935cc7b 100644
--- a/src/EosToQLab.Infrastructure/Import/Esf3d/Esf3dCueHeaderDecoder.cs
+++ b/src/EosToQLab.Infrastructure/Import/EosShowArchive/EosShowArchiveCueHeaderDecoder.cs
@@ -1,15 +1,15 @@
 using System.Globalization;
 using System.Text;
 
-namespace EosToQLab.Infrastructure.Import.Esf3d;
+namespace EosToQLab.Infrastructure.Import.EosShowArchive;
 
-internal static class Esf3dCueHeaderDecoder
+internal static class EosShowArchiveCueHeaderDecoder
 {
     private const byte TextTag = 0x03;
     private const int FollowFieldIndex = 5;
     private const int MetadataFieldCount = 13;
 
-    public static Esf3dCueHeaderDecodeResult Decode(byte[] data, int cueNumberEnd, int recordEnd)
+    public static EosShowArchiveCueHeaderDecodeResult Decode(byte[] data, int cueNumberEnd, int recordEnd)
     {
         var offset = cueNumberEnd;
 
@@ -21,7 +21,7 @@ internal static class Esf3dCueHeaderDecoder
             || !SkipValue(data, ref offset, recordEnd)
             || offset >= recordEnd
             || data[offset++] != 0x02)
-            return Esf3dCueHeaderDecodeResult.Unparsed;
+            return EosShowArchiveCueHeaderDecodeResult.Unparsed;
 
         string? follow = null;
         int? followTextOffset = null;
@@ -31,7 +31,7 @@ internal static class Esf3dCueHeaderDecoder
             if (index != FollowFieldIndex)
             {
                 if (!SkipValue(data, ref offset, recordEnd))
-                    return Esf3dCueHeaderDecodeResult.UnparsedWithFollow(follow, followTextOffset);
+                    return EosShowArchiveCueHeaderDecodeResult.UnparsedWithFollow(follow, followTextOffset);
 
                 continue;
             }
@@ -40,7 +40,7 @@ internal static class Esf3dCueHeaderDecoder
             if (!TryReadFollow(data, offset, recordEnd, out follow, out var followEnd))
             {
                 var unsupported = offset < recordEnd && data[offset] != 0x00 && data[offset] != 0x04;
-                return new Esf3dCueHeaderDecodeResult(
+                return new EosShowArchiveCueHeaderDecodeResult(
                     false,
                     null,
                     unsupported,
@@ -69,7 +69,7 @@ internal static class Esf3dCueHeaderDecoder
                 out var continuationFollow))
             follow = continuationFollow;
 
-        return new Esf3dCueHeaderDecodeResult(
+        return new EosShowArchiveCueHeaderDecodeResult(
             true,
             follow,
             false,
@@ -343,20 +343,20 @@ internal static class Esf3dCueHeaderDecoder
     }
 }
 
-internal sealed record Esf3dCueHeaderDecodeResult(
+internal sealed record EosShowArchiveCueHeaderDecodeResult(
     bool Parsed,
     string? Follow,
     bool FollowDecodeFailed,
     int? CueNotesOffset,
     int? FollowTextOffset)
 {
-    public static Esf3dCueHeaderDecodeResult Unparsed { get; } =
+    public static EosShowArchiveCueHeaderDecodeResult Unparsed { get; } =
         new(false, null, false, null, null);
 
-    public static Esf3dCueHeaderDecodeResult UnparsedWithFollow(
+    public static EosShowArchiveCueHeaderDecodeResult UnparsedWithFollow(
         string? follow,
         int? followTextOffset)
     {
-        return new Esf3dCueHeaderDecodeResult(false, follow, false, null, followTextOffset);
+        return new EosShowArchiveCueHeaderDecodeResult(false, follow, false, null, followTextOffset);
     }
-}
\ No newline at end of file
+}
diff --git a/src/EosToQLab.Infrastructure/Import/Esf3d/Esf3dEosCueImporter.cs b/src/EosToQLab.Infrastructure/Import/EosShowArchive/EosShowArchiveCueImporter.cs
similarity index 79%
rename from src/EosToQLab.Infrastructure/Import/Esf3d/Esf3dEosCueImporter.cs
rename to src/EosToQLab.Infrastructure/Import/EosShowArchive/EosShowArchiveCueImporter.cs
index f3b67a2..fd0103b 100644
--- a/src/EosToQLab.Infrastructure/Import/Esf3d/Esf3dEosCueImporter.cs
+++ b/src/EosToQLab.Infrastructure/Import/EosShowArchive/EosShowArchiveCueImporter.cs
@@ -1,25 +1,34 @@
 using System.Globalization;
 using System.IO.Compression;
 using System.Text;
+using System.Text.Json;
 using EosToQLab.Core.Diagnostics;
 using EosToQLab.Core.Exceptions;
 using EosToQLab.Core.Import;
 using EosToQLab.Core.Models;
 
-namespace EosToQLab.Infrastructure.Import.Esf3d;
+namespace EosToQLab.Infrastructure.Import.EosShowArchive;
 
-public sealed class Esf3dEosCueImporter : IEosCueImporter
+public sealed class EosShowArchiveCueImporter : IEosCueImporter
 {
     private const byte TextTag = 0x03;
     private const int CueScale = 10_000;
     private static readonly byte[] CueMarker = [0x02, 0x01, 0x00, 0x01, 0x01];
     private static readonly byte[] CueRecordTrailer = [0x00, 0x02, 0x02, 0x02, 0x01, 0x00, 0x00, 0x02];
+    private readonly IEosShowArchiveCompatibility _compatibility;
 
-    public EosSourceKind SourceKind => EosSourceKind.Esf3d;
+    public EosShowArchiveCueImporter(IEosShowArchiveCompatibility compatibility)
+    {
+        _compatibility = compatibility ?? throw new ArgumentNullException(nameof(compatibility));
+    }
+
+    public EosSourceKind SourceKind => EosSourceKind.ShowArchive;
 
     public bool CanImport(string fileName)
     {
-        return string.Equals(Path.GetExtension(fileName), ".esf3d", StringComparison.OrdinalIgnoreCase);
+        var extension = Path.GetExtension(fileName);
+        return string.Equals(extension, ".esf3d", StringComparison.OrdinalIgnoreCase)
+               || string.Equals(extension, ".esf2", StringComparison.OrdinalIgnoreCase);
     }
 
     public async Task<EosImportResult> ImportAsync(
@@ -27,19 +36,22 @@ public sealed class Esf3dEosCueImporter : IEosCueImporter
         CancellationToken cancellationToken = default)
     {
         ArgumentNullException.ThrowIfNull(request);
-        var (data, entryName) = await ReadShowDataAsync(request, cancellationToken);
+        var (data, entryName, archiveFormat, archiveVersion) = await ReadArchiveAsync(request, cancellationToken);
         var recovered = ExtractCues(data, ExtractTextFields(data));
         var diagnostics = new List<EosDiagnostic>
         {
-            new Esf3dLossTolerantParsingWarning()
+            new EosShowArchiveLossTolerantParsingWarning()
         };
 
+        if (!_compatibility.IsCovered(archiveFormat, archiveVersion))
+            diagnostics.Add(new EosShowArchiveVersionNotTestedWarning(archiveFormat, archiveVersion));
+
         foreach (var cue in recovered.Where(cue => cue.FollowDecodeFailed))
-            diagnostics.Add(new Esf3dFollowNotDecodedWarning(FormatCueNumber(cue.RawCueNumber)));
+            diagnostics.Add(new EosShowArchiveFollowNotDecodedWarning(FormatCueNumber(cue.RawCueNumber)));
 
         if (recovered.Count == 0)
         {
-            diagnostics.Add(new Esf3dNoCueSequenceWarning());
+            diagnostics.Add(new EosShowArchiveNoCueSequenceWarning());
             diagnostics.Add(new NoCuesFoundWarning());
         }
 
@@ -52,40 +64,46 @@ public sealed class Esf3dEosCueImporter : IEosCueImporter
             Follow = NullIfWhiteSpace(cue.Follow),
             CueNotes = NullIfWhiteSpace(cue.CueNotes),
             SceneText = NullIfWhiteSpace(cue.SceneLabel),
-            SourceKind = EosSourceKind.Esf3d,
+            SourceKind = EosSourceKind.ShowArchive,
             AdditionalValues = new Dictionary<string, string?>(StringComparer.OrdinalIgnoreCase)
             {
                 ["SHOWDAT_OTHER_TEXTS"] = cue.OtherTexts.Count == 0
                     ? null
-                    : string.Join(" | ", cue.OtherTexts)
+                    : string.Join(" | ", cue.OtherTexts),
+                ["EOS_ARCHIVE_FORMAT"] = archiveFormat,
+                ["EOS_ARCHIVE_VERSION"] = archiveVersion
             }
         }).ToArray();
 
+        var archiveType = Path.GetExtension(request.FileName).TrimStart('.').ToUpperInvariant();
+        var versionDescription = string.IsNullOrWhiteSpace(archiveVersion)
+            ? string.Empty
+            : $", EOS {archiveVersion}";
+
         return new EosImportResult(
             cues,
             diagnostics,
             SourceKind,
-            $"ESF3D: {request.FileName} ({entryName})");
+            $"{archiveType}: {request.FileName} ({entryName}{versionDescription})");
     }
 
-    private static async Task<(byte[] Data, string EntryName)> ReadShowDataAsync(
+    private static async Task<(byte[] Data, string EntryName, string? Format, string? Version)> ReadArchiveAsync(
         EosImportRequest request,
         CancellationToken cancellationToken)
     {
         try
         {
             await using var archive = new ZipArchive(request.Content, ZipArchiveMode.Read, true);
-            var entry = archive.Entries
-                .Where(candidate => candidate.FullName.EndsWith("showdat.dat", StringComparison.OrdinalIgnoreCase))
-                .OrderBy(candidate => candidate.FullName.Count(character => character == '/'))
-                .FirstOrDefault();
-
+            var entry = FindShallowestEntry(archive, "showdat.dat");
             if (entry is null) throw new ShowDataEntryMissingException(request.FileName);
 
+            var versionEntry = FindShallowestEntry(archive, "version.json");
+            var (format, version) = await ReadVersionAsync(versionEntry, cancellationToken);
+
             await using var input = await entry.OpenAsync(cancellationToken);
             using var buffer = new MemoryStream(entry.Length > int.MaxValue ? 0 : (int)entry.Length);
             await input.CopyToAsync(buffer, cancellationToken);
-            return (buffer.ToArray(), entry.FullName);
+            return (buffer.ToArray(), entry.FullName, format, version);
         }
         catch (ShowDataEntryMissingException)
         {
@@ -93,7 +111,7 @@ public sealed class Esf3dEosCueImporter : IEosCueImporter
         }
         catch (InvalidDataException exception)
         {
-            throw new Esf3dArchiveInvalidException(request.FileName, exception);
+            throw new EosShowArchiveInvalidException(request.FileName, exception);
         }
         catch (Exception exception) when (exception is IOException or NotSupportedException or ObjectDisposedException)
         {
@@ -101,6 +119,44 @@ public sealed class Esf3dEosCueImporter : IEosCueImporter
         }
     }
 
+
+    private static ZipArchiveEntry? FindShallowestEntry(ZipArchive archive, string fileName)
+    {
+        return archive.Entries
+            .Where(candidate => candidate.FullName.EndsWith(fileName, StringComparison.OrdinalIgnoreCase))
+            .OrderBy(candidate => candidate.FullName.Count(character => character == '/'))
+            .FirstOrDefault();
+    }
+
+    private static async Task<(string? Format, string? Version)> ReadVersionAsync(
+        ZipArchiveEntry? entry,
+        CancellationToken cancellationToken)
+    {
+        if (entry is null) return (null, null);
+
+        try
+        {
+            await using var input = await entry.OpenAsync(cancellationToken);
+            using var document = await JsonDocument.ParseAsync(input, cancellationToken: cancellationToken);
+            var root = document.RootElement;
+            if (root.ValueKind != JsonValueKind.Object) return (null, null);
+
+            var format = root.TryGetProperty("Format", out var formatProperty)
+                         && formatProperty.ValueKind == JsonValueKind.String
+                ? formatProperty.GetString()
+                : null;
+            var version = root.TryGetProperty("Version", out var versionProperty)
+                          && versionProperty.ValueKind == JsonValueKind.String
+                ? versionProperty.GetString()
+                : null;
+            return (NullIfWhiteSpace(format), NullIfWhiteSpace(version));
+        }
+        catch (Exception exception) when (exception is JsonException or IOException or NotSupportedException)
+        {
+            return (null, null);
+        }
+    }
+
     private static List<TextField> ExtractTextFields(byte[] data, int maxCharacters = 65_535)
     {
         var candidates = new List<TextField>();
@@ -165,7 +221,7 @@ public sealed class Esf3dEosCueImporter : IEosCueImporter
                                 && IsUsefulRecordText(field.Text))
                 .ToArray();
             var cueLabel = recordFields.FirstOrDefault(field => field.Offset == number.End);
-            var header = Esf3dCueHeaderDecoder.Decode(data, number.End, recordEnd);
+            var header = EosShowArchiveCueHeaderDecoder.Decode(data, number.End, recordEnd);
             var sceneLabel = recordFields.FirstOrDefault(field =>
                 field.Offset > number.End
                 && field.Offset > 0
@@ -226,7 +282,7 @@ public sealed class Esf3dEosCueImporter : IEosCueImporter
                 // the fully decoded cue header is sufficient to keep the record candidate.
                 var hasCanonicalTrailer =
                     TryFindCueRecordEnd(data, end, recordBoundary, out _);
-                var header = Esf3dCueHeaderDecoder.Decode(data, end, recordBoundary);
+                var header = EosShowArchiveCueHeaderDecoder.Decode(data, end, recordBoundary);
                 if (hasCanonicalTrailer || header.Parsed)
                     result.Add(new NumberCandidate(
                         markerOffset,
@@ -460,4 +516,4 @@ public sealed class Esf3dEosCueImporter : IEosCueImporter
         string? Follow,
         bool FollowDecodeFailed,
         IReadOnlyList<string> OtherTexts);
-}
\ No newline at end of file
+}
diff --git a/tests/EosToQLab.Tests/Core/Diagnostics/Esf3dFollowNotDecodedWarningTests.cs b/tests/EosToQLab.Tests/Core/Diagnostics/EosShowArchiveFollowNotDecodedWarningTests.cs
similarity index 58%
rename from tests/EosToQLab.Tests/Core/Diagnostics/Esf3dFollowNotDecodedWarningTests.cs
rename to tests/EosToQLab.Tests/Core/Diagnostics/EosShowArchiveFollowNotDecodedWarningTests.cs
index 0552e7d..e94a19a 100644
--- a/tests/EosToQLab.Tests/Core/Diagnostics/Esf3dFollowNotDecodedWarningTests.cs
+++ b/tests/EosToQLab.Tests/Core/Diagnostics/EosShowArchiveFollowNotDecodedWarningTests.cs
@@ -1,13 +1,13 @@
 namespace EosToQLab.Tests.Core.Diagnostics;
 
-public sealed class Esf3dFollowNotDecodedWarningTests
+public sealed class EosShowArchiveFollowNotDecodedWarningTests
 {
     [Fact]
     public void Exposes_stable_code_warning_severity_and_message()
     {
-        var warning = new Esf3dFollowNotDecodedWarning("3");
+        var warning = new EosShowArchiveFollowNotDecodedWarning("3");
 
-        Assert.Equal("EOS_ESF3D_FOLLOW_NOT_DECODED", warning.Code);
+        Assert.Equal("EOS_SHOW_ARCHIVE_FOLLOW_NOT_DECODED", warning.Code);
         Assert.Equal(DiagnosticSeverity.Warning, warning.Severity);
         Assert.Contains("3", warning.Message, StringComparison.OrdinalIgnoreCase);
     }
diff --git a/tests/EosToQLab.Tests/Core/Diagnostics/Esf3dLossTolerantParsingWarningTests.cs b/tests/EosToQLab.Tests/Core/Diagnostics/EosShowArchiveLossTolerantParsingWarningTests.cs
similarity index 58%
rename from tests/EosToQLab.Tests/Core/Diagnostics/Esf3dLossTolerantParsingWarningTests.cs
rename to tests/EosToQLab.Tests/Core/Diagnostics/EosShowArchiveLossTolerantParsingWarningTests.cs
index 4432bdf..35688bb 100644
--- a/tests/EosToQLab.Tests/Core/Diagnostics/Esf3dLossTolerantParsingWarningTests.cs
+++ b/tests/EosToQLab.Tests/Core/Diagnostics/EosShowArchiveLossTolerantParsingWarningTests.cs
@@ -1,13 +1,13 @@
 namespace EosToQLab.Tests.Core.Diagnostics;
 
-public sealed class Esf3dLossTolerantParsingWarningTests
+public sealed class EosShowArchiveLossTolerantParsingWarningTests
 {
     [Fact]
     public void Exposes_stable_code_warning_severity_and_message()
     {
-        var warning = new Esf3dLossTolerantParsingWarning();
+        var warning = new EosShowArchiveLossTolerantParsingWarning();
 
-        Assert.Equal("EOS_ESF3D_LOSS_TOLERANT_PARSE", warning.Code);
+        Assert.Equal("EOS_SHOW_ARCHIVE_LOSS_TOLERANT_PARSE", warning.Code);
         Assert.Equal(DiagnosticSeverity.Warning, warning.Severity);
         Assert.Contains("loss-tolerant", warning.Message, StringComparison.OrdinalIgnoreCase);
     }
diff --git a/tests/EosToQLab.Tests/Core/Diagnostics/Esf3dNoCueSequenceWarningTests.cs b/tests/EosToQLab.Tests/Core/Diagnostics/EosShowArchiveNoCueSequenceWarningTests.cs
similarity index 60%
rename from tests/EosToQLab.Tests/Core/Diagnostics/Esf3dNoCueSequenceWarningTests.cs
rename to tests/EosToQLab.Tests/Core/Diagnostics/EosShowArchiveNoCueSequenceWarningTests.cs
index 1232f79..3e79df4 100644
--- a/tests/EosToQLab.Tests/Core/Diagnostics/Esf3dNoCueSequenceWarningTests.cs
+++ b/tests/EosToQLab.Tests/Core/Diagnostics/EosShowArchiveNoCueSequenceWarningTests.cs
@@ -1,13 +1,13 @@
 namespace EosToQLab.Tests.Core.Diagnostics;
 
-public sealed class Esf3dNoCueSequenceWarningTests
+public sealed class EosShowArchiveNoCueSequenceWarningTests
 {
     [Fact]
     public void Exposes_stable_code_warning_severity_and_message()
     {
-        var warning = new Esf3dNoCueSequenceWarning();
+        var warning = new EosShowArchiveNoCueSequenceWarning();
 
-        Assert.Equal("EOS_ESF3D_NO_CUE_SEQUENCE", warning.Code);
+        Assert.Equal("EOS_SHOW_ARCHIVE_NO_CUE_SEQUENCE", warning.Code);
         Assert.Equal(DiagnosticSeverity.Warning, warning.Severity);
         Assert.Contains("monotonic", warning.Message, StringComparison.OrdinalIgnoreCase);
     }
diff --git a/tests/EosToQLab.Tests/Core/Diagnostics/EosShowArchiveVersionNotTestedWarningTests.cs b/tests/EosToQLab.Tests/Core/Diagnostics/EosShowArchiveVersionNotTestedWarningTests.cs
new file mode 100644
index 0000000..2a25f35
--- /dev/null
+++ b/tests/EosToQLab.Tests/Core/Diagnostics/EosShowArchiveVersionNotTestedWarningTests.cs
@@ -0,0 +1,25 @@
+namespace EosToQLab.Tests.Core.Diagnostics;
+
+public sealed class EosShowArchiveVersionNotTestedWarningTests
+{
+    [Fact]
+    public void Describes_an_unknown_archive_version_with_stable_diagnostic_metadata()
+    {
+        var warning = new EosShowArchiveVersionNotTestedWarning("format-id", "4.0.0.1");
+
+        Assert.Equal("EOS_SHOW_ARCHIVE_VERSION_NOT_TESTED", warning.Code);
+        Assert.Equal(DiagnosticSeverity.Warning, warning.Severity);
+        Assert.Contains("format-id", warning.Message, StringComparison.Ordinal);
+        Assert.Contains("4.0.0.1", warning.Message, StringComparison.Ordinal);
+        Assert.Contains("may be incompatible", warning.Message, StringComparison.OrdinalIgnoreCase);
+    }
+
+    [Fact]
+    public void Explains_when_version_json_cannot_be_read()
+    {
+        var warning = new EosShowArchiveVersionNotTestedWarning(null, null);
+
+        Assert.Contains("version.json", warning.Message, StringComparison.OrdinalIgnoreCase);
+        Assert.Contains("may be incompatible", warning.Message, StringComparison.OrdinalIgnoreCase);
+    }
+}
diff --git a/tests/EosToQLab.Tests/Core/Exceptions/Esf3dArchiveInvalidExceptionTests.cs b/tests/EosToQLab.Tests/Core/Exceptions/EosShowArchiveInvalidExceptionTests.cs
similarity index 55%
rename from tests/EosToQLab.Tests/Core/Exceptions/Esf3dArchiveInvalidExceptionTests.cs
rename to tests/EosToQLab.Tests/Core/Exceptions/EosShowArchiveInvalidExceptionTests.cs
index d19bde6..ccbe2b4 100644
--- a/tests/EosToQLab.Tests/Core/Exceptions/Esf3dArchiveInvalidExceptionTests.cs
+++ b/tests/EosToQLab.Tests/Core/Exceptions/EosShowArchiveInvalidExceptionTests.cs
@@ -2,15 +2,15 @@ using EosToQLab.Tests.TestDoubles;
 
 namespace EosToQLab.Tests.Core.Exceptions;
 
-public sealed class Esf3dArchiveInvalidExceptionTests
+public sealed class EosShowArchiveInvalidExceptionTests
 {
     [Fact]
     public void Constructor_exposes_stable_diagnostic_details()
     {
         var inner = new InvalidOperationException("inner");
-        var exception = new Esf3dArchiveInvalidException("show.esf3d", inner);
+        var exception = new EosShowArchiveInvalidException("show.esf3d", inner);
 
-        ExceptionAssertions.HasDetails(exception, "EOS_ESF3D_ARCHIVE_INVALID", "show.esf3d");
+        ExceptionAssertions.HasDetails(exception, "EOS_SHOW_ARCHIVE_INVALID", "show.esf3d");
         Assert.Same(inner, exception.InnerException);
     }
 }
\ No newline at end of file
diff --git a/tests/EosToQLab.Tests/Core/Import/EosShowArchiveCompatibilityTests.cs b/tests/EosToQLab.Tests/Core/Import/EosShowArchiveCompatibilityTests.cs
new file mode 100644
index 0000000..c973c2a
--- /dev/null
+++ b/tests/EosToQLab.Tests/Core/Import/EosShowArchiveCompatibilityTests.cs
@@ -0,0 +1,58 @@
+using System.IO.Compression;
+using System.Text.Json;
+using EosToQLab.Tests.TestDoubles;
+
+namespace EosToQLab.Tests.Core.Import;
+
+public sealed class EosShowArchiveCompatibilityTests
+{
+    [Theory]
+    [InlineData("{CB82CC14-5598-4DB1-A1D7-EBC3BE1D1038}", "3.3.5.69", true)]
+    [InlineData(" {cb82cc14-5598-4db1-a1d7-ebc3be1d1038} ", " 3.3.5.69 ", true)]
+    [InlineData("{CB82CC14-5598-4DB1-A1D7-EBC3BE1D1038}", "3.3.5.70", false)]
+    [InlineData("{00000000-0000-0000-0000-000000000000}", "3.3.5.69", false)]
+    [InlineData(null, null, false)]
+    public void Identifies_only_versions_covered_by_the_compatibility_corpus(
+        string? format,
+        string? version,
+        bool expected)
+    {
+        Assert.Equal(expected, EosShowArchiveCompatibility.IsTested(format, version));
+    }
+
+    [Fact]
+    public async Task Every_declared_tested_version_has_a_versioned_golden_fixture()
+    {
+        var fixtureRoot = TestData.FixturePath("Esf3d");
+        var manifests = new HashSet<(string Format, string Version)>();
+
+        foreach (var archivePath in Directory.EnumerateFiles(fixtureRoot, "*.*", SearchOption.AllDirectories)
+                     .Where(path => IsShowArchiveExtension(Path.GetExtension(path))))
+        {
+            await using var stream = File.OpenRead(archivePath);
+            using var archive = new ZipArchive(stream, ZipArchiveMode.Read);
+            var versionEntry = archive.Entries.FirstOrDefault(entry =>
+                entry.FullName.EndsWith("version.json", StringComparison.OrdinalIgnoreCase));
+            if (versionEntry is null) continue;
+
+            await using var input = versionEntry.Open();
+            using var document = await JsonDocument.ParseAsync(input, cancellationToken: TestContext.Current.CancellationToken);
+            manifests.Add((
+                document.RootElement.GetProperty("Format").GetString()!,
+                document.RootElement.GetProperty("Version").GetString()!));
+        }
+
+        var declared = EosShowArchiveCompatibility.TestedArchives.ToHashSet();
+
+        Assert.Equal(
+            declared.OrderBy(item => item.Version, StringComparer.Ordinal),
+            manifests.OrderBy(item => item.Version, StringComparer.Ordinal));
+        Assert.All(manifests, manifest =>
+            Assert.True(EosShowArchiveCompatibility.IsTested(manifest.Format, manifest.Version)));
+    }
+    private static bool IsShowArchiveExtension(string extension)
+    {
+        return string.Equals(extension, ".esf2", StringComparison.OrdinalIgnoreCase)
+               || string.Equals(extension, ".esf3d", StringComparison.OrdinalIgnoreCase);
+    }
+}
diff --git a/tests/EosToQLab.Tests/Core/Import/IEosShowArchiveCompatibilityTests.cs b/tests/EosToQLab.Tests/Core/Import/IEosShowArchiveCompatibilityTests.cs
new file mode 100644
index 0000000..ef1afd8
--- /dev/null
+++ b/tests/EosToQLab.Tests/Core/Import/IEosShowArchiveCompatibilityTests.cs
@@ -0,0 +1,16 @@
+namespace EosToQLab.Tests.Core.Import;
+
+public sealed class IEosShowArchiveCompatibilityTests
+{
+    [Fact]
+    public void Declares_a_format_and_version_coverage_query()
+    {
+        var method = Assert.Single(typeof(IEosShowArchiveCompatibility).GetMethods());
+
+        Assert.Equal(nameof(IEosShowArchiveCompatibility.IsCovered), method.Name);
+        Assert.Equal(typeof(bool), method.ReturnType);
+        Assert.Equal(
+            [typeof(string), typeof(string)],
+            method.GetParameters().Select(parameter => parameter.ParameterType));
+    }
+}
diff --git a/tests/EosToQLab.Tests/Fixtures/EosShowArchive/Compatibility/eos-3.3.5.69/README.md b/tests/EosToQLab.Tests/Fixtures/EosShowArchive/Compatibility/eos-3.3.5.69/README.md
new file mode 100644
index 0000000..944dc1f
--- /dev/null
+++ b/tests/EosToQLab.Tests/Fixtures/EosShowArchive/Compatibility/eos-3.3.5.69/README.md
@@ -0,0 +1,3 @@
+# EOS 3.3.5.69 compatibility contract
+
+This artificial show archive is the compatibility contract for the exact `Format` and `Version` pair stored in its `version.json`. It contains no production show data.
diff --git a/tests/EosToQLab.Tests/Fixtures/EosShowArchive/Compatibility/eos-3.3.5.69/expected.json b/tests/EosToQLab.Tests/Fixtures/EosShowArchive/Compatibility/eos-3.3.5.69/expected.json
new file mode 100644
index 0000000..975ec20
--- /dev/null
+++ b/tests/EosToQLab.Tests/Fixtures/EosShowArchive/Compatibility/eos-3.3.5.69/expected.json
@@ -0,0 +1,54 @@
+{
+  "archive": {
+    "format": "{CB82CC14-5598-4DB1-A1D7-EBC3BE1D1038}",
+    "version": "3.3.5.69"
+  },
+  "fixtureVersion": "synthetic-known-3.3.5_Build_69",
+  "purpose": "Stable parser contract independent of an installed ETC Eos version.",
+  "cues": [
+    {
+      "sourceOrder": 0,
+      "listNumber": 1,
+      "cueNumber": "1",
+      "label": "Blackout",
+      "notes": "Network note",
+      "scene": "Scene A",
+      "follow": "F3"
+    },
+    {
+      "sourceOrder": 1,
+      "listNumber": 1,
+      "cueNumber": "2",
+      "follow": "H1.5"
+    },
+    {
+      "sourceOrder": 2,
+      "listNumber": 1,
+      "cueNumber": "3"
+    },
+    {
+      "sourceOrder": 3,
+      "listNumber": 1,
+      "cueNumber": "4",
+      "follow": "F1"
+    },
+    {
+      "sourceOrder": 4,
+      "listNumber": 1,
+      "cueNumber": "5",
+      "label": "Only Lable"
+    },
+    {
+      "sourceOrder": 5,
+      "listNumber": 1,
+      "cueNumber": "6",
+      "notes": "Only Notes"
+    },
+    {
+      "sourceOrder": 6,
+      "listNumber": 1,
+      "cueNumber": "7",
+      "scene": "Only Scene"
+    }
+  ]
+}
diff --git a/tests/EosToQLab.Tests/Fixtures/Esf3d/known-3.3.5_Build_69/synthetic-known-3.3.5_Build_69.esf3d b/tests/EosToQLab.Tests/Fixtures/EosShowArchive/Compatibility/eos-3.3.5.69/parser-contract.esf3d
similarity index 100%
rename from tests/EosToQLab.Tests/Fixtures/Esf3d/known-3.3.5_Build_69/synthetic-known-3.3.5_Build_69.esf3d
rename to tests/EosToQLab.Tests/Fixtures/EosShowArchive/Compatibility/eos-3.3.5.69/parser-contract.esf3d
diff --git a/tests/EosToQLab.Tests/Fixtures/EosShowArchive/README.md b/tests/EosToQLab.Tests/Fixtures/EosShowArchive/README.md
new file mode 100644
index 0000000..41e7f31
--- /dev/null
+++ b/tests/EosToQLab.Tests/Fixtures/EosShowArchive/README.md
@@ -0,0 +1,17 @@
+# EOS show archive fixtures
+
+The fixture tree separates compatibility evidence from parser regression and synthetic data:
+
+- `Compatibility/<version>/` contains one real `.esf2` or `.esf3d` archive, one `expected.json` contract, and optional documentation. These directories are the only input used to generate the runtime compatibility catalog.
+- `Regression/` contains real archives that reproduce specific parser defects. They are tested but never advertised as additional supported versions.
+- `Synthetic/` contains generated archives for isolated binary-format cases. They are not compatibility evidence.
+
+A compatibility `expected.json` must contain an `archive` object whose `format` and `version` exactly match the archive's `version.json`. The build fails when either side is missing or disagrees.
+
+When ETC releases a new Eos version:
+
+1. Create a minimal artificial show that exercises labels, notes, scene text, cue-number fractions, Follow, and Hang.
+2. Export it as `.esf2` or `.esf3d`.
+3. Add a new directory below `Compatibility/` without replacing older fixtures.
+4. Record the independently known expected cues and the archive identity in `expected.json`.
+5. Run the full test suite. A published package is produced only after the contract passes.
diff --git a/tests/EosToQLab.Tests/Fixtures/Esf3d/dense-cue-record-variants-3.3.5.esf3d b/tests/EosToQLab.Tests/Fixtures/EosShowArchive/Regression/dense-cue-records/dense-cue-record-variants-3.3.5.69.esf3d
similarity index 100%
rename from tests/EosToQLab.Tests/Fixtures/Esf3d/dense-cue-record-variants-3.3.5.esf3d
rename to tests/EosToQLab.Tests/Fixtures/EosShowArchive/Regression/dense-cue-records/dense-cue-record-variants-3.3.5.69.esf3d
diff --git a/tests/EosToQLab.Tests/Fixtures/Esf3d/known-3.3.5_Build_69-with-901-label/expected.json b/tests/EosToQLab.Tests/Fixtures/EosShowArchive/Regression/dense-cue-records/expected.json
similarity index 91%
rename from tests/EosToQLab.Tests/Fixtures/Esf3d/known-3.3.5_Build_69-with-901-label/expected.json
rename to tests/EosToQLab.Tests/Fixtures/EosShowArchive/Regression/dense-cue-records/expected.json
index ce680ae..a3213aa 100644
--- a/tests/EosToQLab.Tests/Fixtures/Esf3d/known-3.3.5_Build_69-with-901-label/expected.json
+++ b/tests/EosToQLab.Tests/Fixtures/EosShowArchive/Regression/dense-cue-records/expected.json
@@ -1,4 +1,8 @@
 {
+  "archive": {
+    "format": "{CB82CC14-5598-4DB1-A1D7-EBC3BE1D1038}",
+    "version": "3.3.5.69"
+  },
   "fixtureVersion": "synthetic-known-3.3.5_Build_69-with-901-label",
   "purpose": "A real labelled cue 901 must be imported once while the effect/reference records are ignored.",
   "cues": [
diff --git a/tests/EosToQLab.Tests/Fixtures/EosShowArchive/Regression/real-cue-901-labelled/expected.json b/tests/EosToQLab.Tests/Fixtures/EosShowArchive/Regression/real-cue-901-labelled/expected.json
new file mode 100644
index 0000000..eae9a23
--- /dev/null
+++ b/tests/EosToQLab.Tests/Fixtures/EosShowArchive/Regression/real-cue-901-labelled/expected.json
@@ -0,0 +1,60 @@
+{
+  "archive": {
+    "format": "{CB82CC14-5598-4DB1-A1D7-EBC3BE1D1038}",
+    "version": "3.3.5.69"
+  },
+  "fixtureVersion": "synthetic-known-3.3.5_Build_69-with-901-label",
+  "purpose": "A real labelled cue 901 must be imported once while the effect/reference records are ignored.",
+  "cues": [
+    {
+      "sourceOrder": 0,
+      "listNumber": 1,
+      "cueNumber": "1",
+      "label": "Blackout",
+      "notes": "Network note",
+      "scene": "Scene A",
+      "follow": "F3"
+    },
+    {
+      "sourceOrder": 1,
+      "listNumber": 1,
+      "cueNumber": "2",
+      "follow": "H1.5"
+    },
+    {
+      "sourceOrder": 2,
+      "listNumber": 1,
+      "cueNumber": "3"
+    },
+    {
+      "sourceOrder": 3,
+      "listNumber": 1,
+      "cueNumber": "4",
+      "follow": "F1"
+    },
+    {
+      "sourceOrder": 4,
+      "listNumber": 1,
+      "cueNumber": "5",
+      "label": "Only Lable"
+    },
+    {
+      "sourceOrder": 5,
+      "listNumber": 1,
+      "cueNumber": "6",
+      "notes": "Only Notes"
+    },
+    {
+      "sourceOrder": 6,
+      "listNumber": 1,
+      "cueNumber": "7",
+      "scene": "Only Scene"
+    },
+    {
+      "sourceOrder": 7,
+      "listNumber": 1,
+      "cueNumber": "901",
+      "label": "test"
+    }
+  ]
+}
diff --git a/tests/EosToQLab.Tests/Fixtures/Esf3d/known-3.3.5_Build_69-with-901-label/synthetic-known-3.3.5_Build_69-with-901-label.esf3d b/tests/EosToQLab.Tests/Fixtures/EosShowArchive/Regression/real-cue-901-labelled/synthetic-known-3.3.5_Build_69-with-901-label.esf3d
similarity index 100%
rename from tests/EosToQLab.Tests/Fixtures/Esf3d/known-3.3.5_Build_69-with-901-label/synthetic-known-3.3.5_Build_69-with-901-label.esf3d
rename to tests/EosToQLab.Tests/Fixtures/EosShowArchive/Regression/real-cue-901-labelled/synthetic-known-3.3.5_Build_69-with-901-label.esf3d
diff --git a/tests/EosToQLab.Tests/Fixtures/Esf3d/known-3.3.5_Build_69-with-901/expected.json b/tests/EosToQLab.Tests/Fixtures/EosShowArchive/Regression/real-cue-901/expected.json
similarity index 89%
rename from tests/EosToQLab.Tests/Fixtures/Esf3d/known-3.3.5_Build_69-with-901/expected.json
rename to tests/EosToQLab.Tests/Fixtures/EosShowArchive/Regression/real-cue-901/expected.json
index 47dbda1..580423d 100644
--- a/tests/EosToQLab.Tests/Fixtures/Esf3d/known-3.3.5_Build_69-with-901/expected.json
+++ b/tests/EosToQLab.Tests/Fixtures/EosShowArchive/Regression/real-cue-901/expected.json
@@ -1,4 +1,8 @@
 {
+  "archive": {
+    "format": "{CB82CC14-5598-4DB1-A1D7-EBC3BE1D1038}",
+    "version": "3.3.5.69"
+  },
   "fixtureVersion": "synthetic-known-3.3.5_Build_69-with-901",
   "purpose": "A real cue 901 without a label must remain distinguishable from effect/reference records that also use 901-903.",
   "cues": [
@@ -32,7 +36,7 @@
       "sourceOrder": 4,
       "listNumber": 1,
       "cueNumber": "5",
-      "label": "Only Label"
+      "label": "Only Lable"
     },
     {
       "sourceOrder": 5,
diff --git a/tests/EosToQLab.Tests/Fixtures/Esf3d/known-3.3.5_Build_69-with-901/synthetic-known-3.3.5_Build_69-with-901.esf3d b/tests/EosToQLab.Tests/Fixtures/EosShowArchive/Regression/real-cue-901/synthetic-known-3.3.5_Build_69-with-901.esf3d
similarity index 100%
rename from tests/EosToQLab.Tests/Fixtures/Esf3d/known-3.3.5_Build_69-with-901/synthetic-known-3.3.5_Build_69-with-901.esf3d
rename to tests/EosToQLab.Tests/Fixtures/EosShowArchive/Regression/real-cue-901/synthetic-known-3.3.5_Build_69-with-901.esf3d
diff --git a/tests/EosToQLab.Tests/Fixtures/Esf3d/Every_declared_tested_version_has_a_versioned_golden_fixture.esf3d b/tests/EosToQLab.Tests/Fixtures/Esf3d/Every_declared_tested_version_has_a_versioned_golden_fixture.esf3d
new file mode 100644
index 0000000..c5dcc05
Binary files /dev/null and b/tests/EosToQLab.Tests/Fixtures/Esf3d/Every_declared_tested_version_has_a_versioned_golden_fixture.esf3d differ
diff --git a/tests/EosToQLab.Tests/Fixtures/Esf3d/README.md b/tests/EosToQLab.Tests/Fixtures/Esf3d/README.md
deleted file mode 100644
index 8e4311e..0000000
--- a/tests/EosToQLab.Tests/Fixtures/Esf3d/README.md
+++ /dev/null
@@ -1,260 +0,0 @@
-# EosToQLab
-
-A small macOS desktop application built with C#, .NET 10, .NET MAUI, and Blazor Hybrid. It imports ETC Eos cue data from
-either an EOS CSV export or an `.esf3d` archive and creates QLab 5 Network cues in an **already open** QLab workspace.
-
-The application does not export an intermediate file and does not use AppleScript.
-
-## Main behavior
-
-- Accepts `.csv` and `.esf3d` through the native macOS file picker. The app registers `.esf3d` as an imported document
-  type so the extension remains selectable.
-- Selects the import strategy through `IEosCueImporterFactory`.
-- Both source implementations implement the same `IEosCueImporter` contract.
-- Maps both formats into the same `EosCue` domain model.
-- Uses an attribute-bound `EosCsvCue` source model for EOS CSV columns.
-- Opens `.esf3d` as ZIP and reads `showdat.dat` directly from the archive.
-- Uses QLab's OSC-over-TCP interface on `127.0.0.1:53000`.
-- Only lists and targets QLab workspaces that are already open.
-- Never creates, opens, or saves-as a new QLab document.
-- Creates a separate temporary cue list before touching an existing list.
-- Creates imported cues without a QLab number first, then assigns each Network cue exactly its EOS cue number. If that
-  number is already used elsewhere in the workspace, the QLab cue stays unnumbered; the importer never increments or
-  invents a replacement number. The EOS list/cue values sent by the Network cue are unchanged.
-- Rejects a duplicate cue-list name by default.
-- Allows replacement only after explicit confirmation in the UI and a second enforcement check in the QLab service.
-- Maps EOS scene text to QLab Memo-cue names; generated Memo notes stay empty.
-- Maps EOS labels to Network-cue names and EOS cue notes to Network-cue notes without generated prefixes.
-- Models the QLab EOS Network-cue parameter stack explicitly as Type, Specify user, optional User, Command, and
-  command-specific arguments. The current import uses Cues → Run cue in specific list → List/Cue.
-- Corrects Follow/Hang handling, reads `FOLLOW` by column name in CSV, and decodes both legacy and current ESF3D
-  continuation objects. Automatically triggered cues can be excluded or imported disarmed.
-- Uses dedicated exception and warning types with English messages and stable diagnostic codes.
-
-## Small project structure
-
-```text
-src/
-├── EosToQLab.Core
-│   ├── Models
-│   ├── Import contracts
-│   ├── QLab contracts
-│   ├── Planning
-│   ├── Diagnostics
-│   └── Exceptions
-├── EosToQLab.Infrastructure
-│   ├── CSV import
-│   ├── ESF3D import
-│   └── QLab OSC
-└── EosToQLab.Application
-    └── MAUI Blazor Hybrid UI for macOS
-
-tests/
-└── EosToQLab.Tests
-    ├── one xUnit test file per production type
-    ├── versioned ESF3D compatibility fixtures
-    └── Coverlet line-coverage gate
-```
-
-The structure is intentionally limited to three production projects. There is no separate project for every technical
-layer.
-
-## Application icon
-
-`src/EosToQLab.Application/Resources/AppIcon/appicon.svg` is the single MAUI application-icon source. It is a vector
-recreation of the original EOS-to-QLab PNG artwork and contains only paths and shapes, so the MAUI resizetizer does not
-depend on an installed font. The same SVG is mirrored as `.idea/icon.svg` for JetBrains Rider project branding.
-
-## Import strategy factory
-
-```csharp
-public interface IEosCueImporter
-{
-    EosSourceKind SourceKind { get; }
-    bool CanImport(string fileName);
-    Task<EosImportResult> ImportAsync(
-        EosImportRequest request,
-        CancellationToken cancellationToken = default);
-}
-```
-
-`CsvEosCueImporter` and `Esf3dEosCueImporter` implement this interface. `EosCueImporterFactory` selects the first
-registered strategy whose `CanImport` method accepts the file.
-
-## CSV mapping
-
-`EosCsvCue` is a transport/source model. Its properties are associated with EOS column names using
-`EosCsvColumnAttribute`:
-
-```csharp
-[EosCsvColumn("TARGET_ID", Required = true)]
-public string TargetId { get; init; } = string.Empty;
-
-[EosCsvColumn("SCENE_TEXT")]
-public string? SceneText { get; init; }
-```
-
-The binder caches reflection metadata once per source type, reads values by header name, converts them, and then maps
-the source rows into the common `EosCue` model. Cue-part rows are grouped before mapping.
-
-## ESF3D parser scope
-
-`showdat.dat` is a proprietary format. The included parser is intentionally loss-tolerant and is based on the supplied
-Python reference parser. It currently recovers:
-
-- cue numbers encoded as fixed-point values with scale 10,000;
-- cue labels directly following the encoded cue number;
-- cue notes from the dedicated cue-header field;
-- nested scene labels;
-- known scalar, textual, compact-object, and current continuation-object Follow/Hang encodings;
-- other short text fields as additional diagnostic data.
-
-Unknown Follow/Hang object variants are not guessed. They produce a cue-specific warning and remain empty instead of
-being interpreted as another field.
-
-## Follow/Hang import policy
-
-The plan builder tracks automatic playback chains separately for every EOS cue-list number. When a cue has Follow or
-Hang, the next cue is considered automatically triggered; if that next cue also has Follow/Hang, the chain continues.
-The UI offers two policies:
-
-- **Do not import**: automatically triggered cues are omitted completely.
-- **Import disarmed**: automatically triggered Network cues are created with `armed = false`.
-
-The cue that starts the chain remains a normal imported cue. For example, `83.1 F1`, `83.2 F1`, `83.3 F1`, `84` affects
-83.2, 83.3, and 84.
-
-## EOS Network cue parameters
-
-`QLabEosNetworkCommand` represents the visible EOS Network-cue fields in order. It uses string-backed target and command
-value types so additional QLab menu values can be added without changing the OSC session API. For the current import it
-emits:
-
-1. Type = `Cues`
-2. Specify user = `No` (or `Yes` plus a User field)
-3. Command = `Run cue in specific list`
-4. List = the EOS list number
-5. Cue = the EOS cue number
-
-## QLab requirements
-
-Before starting an import:
-
-1. Start QLab 5.
-2. Open the target `.qlab5` workspace manually.
-3. Configure OSC edit access in that workspace. Enter the passcode in EosToQLab if required.
-4. Create a Network Patch of type **ETC Eos family**. The default expected patch name is exactly `Eos`.
-5. Start EosToQLab and refresh the list of open workspaces.
-
-The app does not attempt to create or open a QLab file.
-
-## Existing cue-list protection
-
-The default conflict policy is `Fail`. A matching name is checked twice:
-
-1. in the Blazor UI before the import starts;
-2. again inside `QLabOscService` immediately before any cue list is created.
-
-For an explicitly approved replacement, the service:
-
-1. builds the complete new list under a unique temporary name;
-2. renames the existing list to a unique backup name;
-3. renames the new list to the requested final name;
-4. optionally saves a recoverable state containing both lists;
-5. deletes the backup list;
-6. assigns each Network cue exactly its EOS cue number when that number is now free;
-7. optionally saves the final state.
-
-If a later step fails, the service attempts to restore the old name, restore the previous current cue list, remove the
-temporary import, and save the rollback where necessary.
-
-See [docs/QLAB_SAFETY.md](docs/QLAB_SAFETY.md).
-
-## Development prerequisites
-
-- macOS with a supported Xcode version
-- .NET SDK `10.0.302` or a compatible later patch
-- .NET MAUI workload
-- QLab 5 for integration testing
-
-```bash
-dotnet workload install maui
-dotnet restore EosToQLab.slnx
-```
-
-Run the xUnit suite:
-
-```bash
-dotnet test tests/EosToQLab.Tests/EosToQLab.Tests.csproj -c Release
-```
-
-Run the same 100% line-coverage gate used by CI:
-
-```bash
-dotnet test tests/EosToQLab.Tests/EosToQLab.Tests.csproj -c Release \
-  -p:CollectCoverage=true \
-  -p:CoverletOutputFormat=cobertura \
-  -p:Threshold=100 \
-  -p:ThresholdType=line \
-  -p:ThresholdStat=total \
-  '-p:Include=[EosToQLab.Core]*,[EosToQLab.Infrastructure]*'
-```
-
-Run the app:
-
-```bash
-dotnet build src/EosToQLab.Application/EosToQLab.Application.csproj \
-  -f net10.0-maccatalyst \
-  -t:Run
-```
-
-## Publish a self-contained `.app`
-
-Apple Silicon:
-
-```bash
-./packaging/publish-macos.sh arm64
-```
-
-Intel:
-
-```bash
-./packaging/publish-macos.sh x64
-```
-
-Artifacts are copied to `dist/`. Local output is ad-hoc signed. Public distribution requires an Apple Developer ID and
-notarization.
-
-## Documentation
-
-- [Architecture](docs/ARCHITECTURE.md)
-- [QLab safety and conflict handling](docs/QLAB_SAFETY.md)
-- [Build and validation](docs/BUILD_AND_TEST.md)
-- [Testing strategy and ESF3D compatibility](docs/TESTING.md)
-
-## Import UI
-
-The Mac Catalyst UI keeps each workspace's OSC passcode only in a 24-hour in-process session cache by default, loads
-QLab network patches into a dropdown, and provides an editable cue preview. Passcodes are not written to disk and are
-lost when the app process exits. The optional `MauiSecurePasscodeStore` remains available for signed builds that can use
-the platform keychain. Label, notes, and scene text can be changed before import, and cues can be selected individually
-or through a tri-state Select all control.
-
-## Mac Catalyst icon and Rider restart troubleshooting
-
-The MAUI icon source is `src/EosToQLab.Application/Resources/AppIcon/appicon.svg`.
-Mac Catalyst references the generated asset catalog through
-`XSAppIconAssets = Assets.xcassets/appicon.appiconset` in `Info.plist`.
-The same SVG is stored as `.idea/icon.svg` for Rider's project icon; this is
-separate from the icon displayed for the built `.app` process.
-
-After changing the application icon, remove `bin` and `obj` before rebuilding.
-If Rider is stopped while UIKit is still closing the previous Mac Catalyst
-scene and the next launch fails in `UIKitMacHelper`, run:
-
-```bash
-./scripts/reset-maccatalyst-debug-state.sh
-```
-
-The script only removes debug build output and saved window/scene state. It does
-not delete the whole application container.
diff --git a/tests/EosToQLab.Tests/Fixtures/Esf3d/known-3.3.5_Build_69/expected.json b/tests/EosToQLab.Tests/Fixtures/Esf3d/expected.json
similarity index 90%
rename from tests/EosToQLab.Tests/Fixtures/Esf3d/known-3.3.5_Build_69/expected.json
rename to tests/EosToQLab.Tests/Fixtures/Esf3d/expected.json
index 7803f68..590e781 100644
--- a/tests/EosToQLab.Tests/Fixtures/Esf3d/known-3.3.5_Build_69/expected.json
+++ b/tests/EosToQLab.Tests/Fixtures/Esf3d/expected.json
@@ -1,4 +1,8 @@
 {
+  "archive": {
+    "format": "{CB82CC14-5598-4DB1-A1D7-EBC3BE1D1038}",
+    "version": "3.3.5.69"
+  },
   "fixtureVersion": "synthetic-known-3.3.5_Build_69",
   "purpose": "Stable parser contract independent of an installed ETC Eos version.",
   "cues": [
diff --git a/tests/EosToQLab.Tests/Infrastructure/Import/EosShowArchive/EmbeddedEosShowArchiveCompatibilityTests.cs b/tests/EosToQLab.Tests/Infrastructure/Import/EosShowArchive/EmbeddedEosShowArchiveCompatibilityTests.cs
new file mode 100644
index 0000000..218de8f
--- /dev/null
+++ b/tests/EosToQLab.Tests/Infrastructure/Import/EosShowArchive/EmbeddedEosShowArchiveCompatibilityTests.cs
@@ -0,0 +1,120 @@
+using System.Text;
+using System.Text.Json;
+using EosToQLab.Tests.TestDoubles;
+
+namespace EosToQLab.Tests.Infrastructure.Import.EosShowArchive;
+
+public sealed class EmbeddedEosShowArchiveCompatibilityTests
+{
+    [Fact]
+    public void Loads_the_generated_catalog_and_matches_format_version_pairs_case_insensitively()
+    {
+        var sut = new EmbeddedEosShowArchiveCompatibility(Catalog(
+            """
+            {
+              "schemaVersion": 1,
+              "archives": [
+                { "format": " format-id ", "version": " 3.3.5.69 ", "fixture": "eos-3.3.5.69" }
+              ]
+            }
+            """));
+
+        Assert.True(sut.IsCovered("FORMAT-ID", "3.3.5.69"));
+        Assert.True(sut.IsCovered(" format-id ", " 3.3.5.69 "));
+        Assert.False(sut.IsCovered("format-id", "3.3.5.70"));
+        Assert.False(sut.IsCovered(null, "3.3.5.69"));
+        Assert.False(sut.IsCovered("format-id", " "));
+    }
+
+    [Fact]
+    public void Generated_catalog_matches_exactly_the_compatibility_fixture_contracts()
+    {
+        var fixtureRoot = TestData.FixturePath("EosShowArchive", "Compatibility");
+        var expected = Directory.EnumerateDirectories(fixtureRoot)
+            .Select(directory => Path.Combine(directory, "expected.json"))
+            .Select(File.ReadAllText)
+            .Select(JsonDocument.Parse)
+            .Select(document =>
+            {
+                using (document)
+                {
+                    var archive = document.RootElement.GetProperty("archive");
+                    return $"{archive.GetProperty("format").GetString()}|{archive.GetProperty("version").GetString()}";
+                }
+            })
+            .OrderBy(value => value, StringComparer.OrdinalIgnoreCase)
+            .ToArray();
+
+        using var catalogStream = typeof(EmbeddedEosShowArchiveCompatibility).Assembly
+            .GetManifestResourceStream(EmbeddedEosShowArchiveCompatibility.ResourceName);
+        Assert.NotNull(catalogStream);
+        using var catalog = JsonDocument.Parse(catalogStream);
+        var actual = catalog.RootElement.GetProperty("archives")
+            .EnumerateArray()
+            .Select(entry => $"{entry.GetProperty("format").GetString()}|{entry.GetProperty("version").GetString()}")
+            .OrderBy(value => value, StringComparer.OrdinalIgnoreCase)
+            .ToArray();
+
+        Assert.Equal(expected, actual);
+    }
+
+    [Fact]
+    public void Default_constructor_loads_the_build_generated_embedded_resource()
+    {
+        var sut = new EmbeddedEosShowArchiveCompatibility();
+
+        Assert.True(sut.IsCovered(
+            "{CB82CC14-5598-4DB1-A1D7-EBC3BE1D1038}",
+            "3.3.5.69"));
+        Assert.False(sut.IsCovered(
+            "{CB82CC14-5598-4DB1-A1D7-EBC3BE1D1038}",
+            "99.0"));
+    }
+
+    [Theory]
+    [InlineData("{}")]
+    [InlineData("{ \"schemaVersion\": 2, \"archives\": [] }")]
+    [InlineData("{ \"schemaVersion\": 1 }")]
+    [InlineData("{ \"schemaVersion\": 1, \"archives\": {} }")]
+    public void Rejects_unsupported_catalog_schemas(string json)
+    {
+        Assert.Throws<InvalidDataException>(() =>
+            new EmbeddedEosShowArchiveCompatibility(Catalog(json)));
+    }
+
+    [Theory]
+    [InlineData("null")]
+    [InlineData("{ \"format\": \"x\" }")]
+    [InlineData("{ \"format\": \"\", \"version\": \"1\" }")]
+    [InlineData("{ \"format\": \"x\", \"version\": \" \" }")]
+    public void Rejects_invalid_archive_entries(string entry)
+    {
+        var json = $$"""
+                     {
+                       "schemaVersion": 1,
+                       "archives": [{{entry}}]
+                     }
+                     """;
+
+        Assert.Throws<InvalidDataException>(() =>
+            new EmbeddedEosShowArchiveCompatibility(Catalog(json)));
+    }
+
+    [Fact]
+    public void Rejects_an_assembly_without_the_generated_catalog()
+    {
+        Assert.Throws<InvalidOperationException>(() =>
+            new EmbeddedEosShowArchiveCompatibility(typeof(EmbeddedEosShowArchiveCompatibilityTests).Assembly));
+        Assert.Throws<ArgumentNullException>(() =>
+            new EmbeddedEosShowArchiveCompatibility((System.Reflection.Assembly)null!));
+    }
+
+    [Fact]
+    public void Rejects_a_null_catalog_stream()
+    {
+        Assert.Throws<ArgumentNullException>(() =>
+            new EmbeddedEosShowArchiveCompatibility((Stream)null!));
+    }
+
+    private static MemoryStream Catalog(string json) => new(Encoding.UTF8.GetBytes(json));
+}
diff --git a/tests/EosToQLab.Tests/Infrastructure/Import/Esf3d/Esf3dCueHeaderDecodeResultTests.cs b/tests/EosToQLab.Tests/Infrastructure/Import/EosShowArchive/EosShowArchiveCueHeaderDecodeResultTests.cs
similarity index 59%
rename from tests/EosToQLab.Tests/Infrastructure/Import/Esf3d/Esf3dCueHeaderDecodeResultTests.cs
rename to tests/EosToQLab.Tests/Infrastructure/Import/EosShowArchive/EosShowArchiveCueHeaderDecodeResultTests.cs
index b9d5da9..7d7817a 100644
--- a/tests/EosToQLab.Tests/Infrastructure/Import/Esf3d/Esf3dCueHeaderDecodeResultTests.cs
+++ b/tests/EosToQLab.Tests/Infrastructure/Import/EosShowArchive/EosShowArchiveCueHeaderDecodeResultTests.cs
@@ -1,11 +1,11 @@
-namespace EosToQLab.Tests.Infrastructure.Import.Esf3d;
+namespace EosToQLab.Tests.Infrastructure.Import.EosShowArchive;
 
-public sealed class Esf3dCueHeaderDecodeResultTests
+public sealed class EosShowArchiveCueHeaderDecodeResultTests
 {
     [Fact]
     public void Stores_decoded_header_values()
     {
-        var result = new Esf3dCueHeaderDecodeResult(
+        var result = new EosShowArchiveCueHeaderDecodeResult(
             true,
             "F1",
             false,
@@ -17,6 +17,6 @@ public sealed class Esf3dCueHeaderDecodeResultTests
         Assert.False(result.FollowDecodeFailed);
         Assert.Equal(12, result.CueNotesOffset);
         Assert.Equal(24, result.FollowTextOffset);
-        Assert.False(Esf3dCueHeaderDecodeResult.Unparsed.Parsed);
+        Assert.False(EosShowArchiveCueHeaderDecodeResult.Unparsed.Parsed);
     }
 }
\ No newline at end of file
diff --git a/tests/EosToQLab.Tests/Infrastructure/Import/EosShowArchive/EosShowArchiveCueHeaderDecoderTests.cs b/tests/EosToQLab.Tests/Infrastructure/Import/EosShowArchive/EosShowArchiveCueHeaderDecoderTests.cs
new file mode 100644
index 0000000..a787424
--- /dev/null
+++ b/tests/EosToQLab.Tests/Infrastructure/Import/EosShowArchive/EosShowArchiveCueHeaderDecoderTests.cs
@@ -0,0 +1,117 @@
+using EosToQLab.Tests.TestDoubles;
+
+namespace EosToQLab.Tests.Infrastructure.Import.EosShowArchive;
+
+public sealed class EosShowArchiveCueHeaderDecoderTests
+{
+    [Theory]
+    [InlineData("F1", "F1")]
+    [InlineData("follow: 1.5", "F1.5")]
+    [InlineData("hang 2", "H2")]
+    [InlineData(" h3 ", "H3")]
+    public void Decodes_text_follow_variants(string input, string expected)
+    {
+        var fixture = EosShowArchiveHeaderFixtureBuilder.Build(EosShowArchiveHeaderFixtureBuilder.Text(input), "note");
+        var result = EosShowArchiveCueHeaderDecoder.Decode(fixture.Data, fixture.CueNumberEnd, fixture.RecordEnd);
+        Assert.True(result.Parsed);
+        Assert.Equal(expected, result.Follow);
+        Assert.Equal(fixture.NotesOffset, result.CueNotesOffset);
+        Assert.NotNull(result.FollowTextOffset);
+        Assert.False(result.FollowDecodeFailed);
+    }
+
+    [Fact]
+    public void Decodes_unsigned_boolean_and_compact_object_variants()
+    {
+        Assert.Equal("F1.5", Decode(EosShowArchiveHeaderFixtureBuilder.Unsigned(1500)).Follow);
+        Assert.Equal("H", Decode(EosShowArchiveHeaderFixtureBuilder.Boolean(true)).Follow);
+        Assert.Equal("H1.5", Decode([0x02, .. EosShowArchiveHeaderFixtureBuilder.Boolean(true), .. EosShowArchiveHeaderFixtureBuilder.Unsigned(1500), 0x04]).Follow);
+        Assert.Equal("F1.5", Decode([0x02, .. EosShowArchiveHeaderFixtureBuilder.Unsigned(0), .. EosShowArchiveHeaderFixtureBuilder.Unsigned(1500), 0x04]).Follow);
+    }
+
+    [Fact]
+    public void Decodes_current_continuation_follow_and_hang_when_legacy_field_is_null()
+    {
+        var followFixture = EosShowArchiveHeaderFixtureBuilder.Build(
+            [0x00],
+            continuation: EosShowArchiveHeaderFixtureBuilder.Continuation(false, 1250));
+        var hangFixture = EosShowArchiveHeaderFixtureBuilder.Build(
+            [0x00],
+            continuation: EosShowArchiveHeaderFixtureBuilder.Continuation(true, 1500));
+
+        Assert.Equal(
+            "F1.25",
+            EosShowArchiveCueHeaderDecoder.Decode(followFixture.Data, 0, followFixture.RecordEnd).Follow);
+        Assert.Equal(
+            "H1.5",
+            EosShowArchiveCueHeaderDecoder.Decode(hangFixture.Data, 0, hangFixture.RecordEnd).Follow);
+    }
+
+    [Fact]
+    public void Decodes_real_eos_3_3_5_hang_continuation_bytes()
+    {
+        byte[] continuation =
+        [
+            0x02,
+            0x01, 0x00,
+            0x08, 0x01,
+            0x01, 0x00,
+            0x08, 0x01,
+            0x09, 0xDC, 0x05,
+            0x00,
+            0x08, 0x01
+        ];
+        var fixture = EosShowArchiveHeaderFixtureBuilder.Build([0x00], continuation: continuation);
+
+        var result = EosShowArchiveCueHeaderDecoder.Decode(fixture.Data, 0, fixture.RecordEnd);
+
+        Assert.True(result.Parsed);
+        Assert.Equal("H1.5", result.Follow);
+        Assert.False(result.FollowDecodeFailed);
+    }
+
+    [Fact]
+    public void Keeps_support_for_legacy_boolean_hang_mode()
+    {
+        var fixture = EosShowArchiveHeaderFixtureBuilder.Build(
+            [0x00],
+            continuation: EosShowArchiveHeaderFixtureBuilder.Continuation(
+                false,
+                1250,
+                secondLegacyMode: true));
+
+        var result = EosShowArchiveCueHeaderDecoder.Decode(fixture.Data, 0, fixture.RecordEnd);
+
+        Assert.Equal("H1.25", result.Follow);
+    }
+
+    [Fact]
+    public void Null_follow_is_valid_and_unknown_encoding_is_reported()
+    {
+        var empty = Decode([0x04]);
+        Assert.True(empty.Parsed);
+        Assert.Null(empty.Follow);
+        Assert.False(empty.FollowDecodeFailed);
+
+        var unsupported = Decode([0x07]);
+        Assert.False(unsupported.Parsed);
+        Assert.True(unsupported.FollowDecodeFailed);
+    }
+
+    [Fact]
+    public void Invalid_header_is_unparsed()
+    {
+        var result = EosShowArchiveCueHeaderDecoder.Decode([0x00], 0, 1);
+        Assert.Equal(EosShowArchiveCueHeaderDecodeResult.Unparsed, result);
+        var withFollow = EosShowArchiveCueHeaderDecodeResult.UnparsedWithFollow("F1", 3);
+        Assert.False(withFollow.Parsed);
+        Assert.Equal("F1", withFollow.Follow);
+        Assert.Equal(3, withFollow.FollowTextOffset);
+    }
+
+    private static EosShowArchiveCueHeaderDecodeResult Decode(byte[] follow)
+    {
+        var fixture = EosShowArchiveHeaderFixtureBuilder.Build(follow);
+        return EosShowArchiveCueHeaderDecoder.Decode(fixture.Data, 0, fixture.RecordEnd);
+    }
+}
diff --git a/tests/EosToQLab.Tests/Infrastructure/Import/Esf3d/Esf3dEosCueImporterTests.cs b/tests/EosToQLab.Tests/Infrastructure/Import/EosShowArchive/EosShowArchiveCueImporterTests.cs
similarity index 54%
rename from tests/EosToQLab.Tests/Infrastructure/Import/Esf3d/Esf3dEosCueImporterTests.cs
rename to tests/EosToQLab.Tests/Infrastructure/Import/EosShowArchive/EosShowArchiveCueImporterTests.cs
index 9aa64f0..e9fb927 100644
--- a/tests/EosToQLab.Tests/Infrastructure/Import/Esf3d/Esf3dEosCueImporterTests.cs
+++ b/tests/EosToQLab.Tests/Infrastructure/Import/EosShowArchive/EosShowArchiveCueImporterTests.cs
@@ -2,23 +2,24 @@ using System.IO.Compression;
 using System.Text.Json;
 using EosToQLab.Tests.TestDoubles;
 
-namespace EosToQLab.Tests.Infrastructure.Import.Esf3d;
+namespace EosToQLab.Tests.Infrastructure.Import.EosShowArchive;
 
-public sealed class Esf3dEosCueImporterTests
+public sealed class EosShowArchiveCueImporterTests
 {
-    private readonly Esf3dEosCueImporter _sut = new();
+    private readonly EosShowArchiveCueImporter _sut = new(new FakeEosShowArchiveCompatibility());
 
     public static TheoryData<string, string> CompatibilityFixtures
     {
         get
         {
             var fixtures = new TheoryData<string, string>();
-            var root = TestData.FixturePath("Esf3d");
+            var root = TestData.FixturePath("EosShowArchive", "Compatibility");
             foreach (var directory in Directory.EnumerateDirectories(root)
                          .OrderBy(path => path, StringComparer.Ordinal))
             {
                 var expectedPath = Path.Combine(directory, "expected.json");
-                var archivePath = Directory.EnumerateFiles(directory, "*.esf3d").SingleOrDefault();
+                var archivePath = Directory.EnumerateFiles(directory)
+                    .SingleOrDefault(path => IsShowArchiveExtension(Path.GetExtension(path)));
                 if (File.Exists(expectedPath) && archivePath is not null) fixtures.Add(archivePath, expectedPath);
             }
 
@@ -26,24 +27,32 @@ public sealed class Esf3dEosCueImporterTests
         }
     }
 
+    [Fact]
+    public void Constructor_rejects_a_null_compatibility_service()
+    {
+        Assert.Throws<ArgumentNullException>(() => new EosShowArchiveCueImporter(null!));
+    }
+
     [Fact]
     public void Reports_source_kind_and_supported_extension()
     {
-        Assert.Equal(EosSourceKind.Esf3d, _sut.SourceKind);
+        Assert.Equal(EosSourceKind.ShowArchive, _sut.SourceKind);
         Assert.True(_sut.CanImport("SHOW.ESF3D"));
+        Assert.True(_sut.CanImport("SHOW.ESF2"));
         Assert.False(_sut.CanImport("show.csv"));
     }
 
     [Fact]
     public void Every_versioned_fixture_directory_has_one_archive_and_one_contract()
     {
-        var root = TestData.FixturePath("Esf3d");
+        var root = TestData.FixturePath("EosShowArchive", "Compatibility");
         var directories = Directory.EnumerateDirectories(root).ToArray();
         Assert.NotEmpty(directories);
 
         var incomplete = directories
             .Where(directory =>
-                Directory.EnumerateFiles(directory, "*.esf3d").Count() != 1
+                Directory.EnumerateFiles(directory)
+                    .Count(path => IsShowArchiveExtension(Path.GetExtension(path))) != 1
                 || !File.Exists(Path.Combine(directory, "expected.json")))
             .Select(Path.GetFileName)
             .ToArray();
@@ -59,9 +68,13 @@ public sealed class Esf3dEosCueImporterTests
         using var expected =
             JsonDocument.Parse(await File.ReadAllTextAsync(expectedPath, TestContext.Current.CancellationToken));
 
-        var result = await _sut.ImportAsync(new EosImportRequest(Path.GetFileName(fixture), stream),
+        var sut = new EosShowArchiveCueImporter(new EmbeddedEosShowArchiveCompatibility());
+        var result = await sut.ImportAsync(new EosImportRequest(Path.GetFileName(fixture), stream),
             TestContext.Current.CancellationToken);
         Assert.False(string.IsNullOrWhiteSpace(expected.RootElement.GetProperty("fixtureVersion").GetString()));
+        var expectedArchive = expected.RootElement.GetProperty("archive");
+        var expectedFormat = expectedArchive.GetProperty("format").GetString();
+        var expectedVersion = expectedArchive.GetProperty("version").GetString();
         var expectedCues = expected.RootElement.GetProperty("cues").EnumerateArray().ToArray();
 
         Assert.Equal(expectedCues.Length, result.Cues.Count);
@@ -76,21 +89,95 @@ public sealed class Esf3dEosCueImporterTests
             Assert.Equal(GetOptional(contract, "notes"), actual.CueNotes);
             Assert.Equal(GetOptional(contract, "scene"), actual.SceneText);
             Assert.Equal(GetOptional(contract, "follow"), actual.Follow);
-            Assert.Equal(EosSourceKind.Esf3d, actual.SourceKind);
+            Assert.Equal(EosSourceKind.ShowArchive, actual.SourceKind);
+            Assert.Equal(expectedFormat, actual.AdditionalValues["EOS_ARCHIVE_FORMAT"]);
+            Assert.Equal(expectedVersion, actual.AdditionalValues["EOS_ARCHIVE_VERSION"]);
         }
 
-        Assert.Contains(result.Diagnostics, d => d is Esf3dLossTolerantParsingWarning);
-        Assert.DoesNotContain(result.Diagnostics, d => d is Esf3dFollowNotDecodedWarning);
+        Assert.Contains(result.Diagnostics, d => d is EosShowArchiveLossTolerantParsingWarning);
+        Assert.DoesNotContain(result.Diagnostics, d => d is EosShowArchiveFollowNotDecodedWarning);
+        Assert.DoesNotContain(result.Diagnostics, d => d is EosShowArchiveVersionNotTestedWarning);
         Assert.Contains("showdat.dat", result.SourceDescription, StringComparison.OrdinalIgnoreCase);
     }
 
+    [Fact]
+    public async Task Imports_esf2_without_the_optional_3d_payload()
+    {
+        var fixture = TestData.FixturePath(
+            "EosShowArchive",
+            "Compatibility",
+            "eos-3.3.5.69",
+            "parser-contract.esf3d");
+        await using var fixtureStream = File.OpenRead(fixture);
+        using var fixtureArchive = new ZipArchive(fixtureStream, ZipArchiveMode.Read);
+        await using var stream = await CreateArchiveFromEntriesAsync(
+            fixtureArchive,
+            "showdat.dat",
+            "version.json");
+
+        var sut = new EosShowArchiveCueImporter(new EmbeddedEosShowArchiveCompatibility());
+        var result = await sut.ImportAsync(
+            new EosImportRequest("synthetic-known-3.3.5_Build_69.esf2", stream),
+            TestContext.Current.CancellationToken);
+
+        Assert.Equal(7, result.Cues.Count);
+        Assert.StartsWith("ESF2:", result.SourceDescription);
+        Assert.Contains("EOS 3.3.5.69", result.SourceDescription, StringComparison.Ordinal);
+        Assert.DoesNotContain(result.Diagnostics, diagnostic => diagnostic is EosShowArchiveVersionNotTestedWarning);
+        Assert.All(result.Cues, cue =>
+        {
+            Assert.Equal("{CB82CC14-5598-4DB1-A1D7-EBC3BE1D1038}", cue.AdditionalValues["EOS_ARCHIVE_FORMAT"]);
+            Assert.Equal("3.3.5.69", cue.AdditionalValues["EOS_ARCHIVE_VERSION"]);
+        });
+    }
+
+    [Fact]
+    public async Task Warns_when_archive_version_has_not_been_verified()
+    {
+        await using var archive = CreateArchiveWithVersion(
+            [],
+            """{ "Format": "{CB82CC14-5598-4DB1-A1D7-EBC3BE1D1038}", "Version": "9.9.9.1" }""");
+
+        var sut = new EosShowArchiveCueImporter(new FakeEosShowArchiveCompatibility((_, _) => false));
+        var result = await sut.ImportAsync(
+            new EosImportRequest("future.esf3d", archive),
+            TestContext.Current.CancellationToken);
+
+        var warning = Assert.Single(result.Diagnostics.OfType<EosShowArchiveVersionNotTestedWarning>());
+        Assert.Equal("{CB82CC14-5598-4DB1-A1D7-EBC3BE1D1038}", warning.Format);
+        Assert.Equal("9.9.9.1", warning.Version);
+    }
+
+    [Theory]
+    [InlineData(null)]
+    [InlineData("not-json")]
+    [InlineData("{}")]
+    [InlineData("[]")]
+    public async Task Warns_when_version_manifest_is_missing_or_unreadable(string? versionJson)
+    {
+        await using var archive = versionJson is null
+            ? CreateArchive([])
+            : CreateArchiveWithVersion([], versionJson);
+
+        var sut = new EosShowArchiveCueImporter(new FakeEosShowArchiveCompatibility((_, _) => false));
+        var result = await sut.ImportAsync(
+            new EosImportRequest("unknown.esf2", archive),
+            TestContext.Current.CancellationToken);
+
+        var warning = Assert.Single(result.Diagnostics.OfType<EosShowArchiveVersionNotTestedWarning>());
+        Assert.Null(warning.Format);
+        Assert.Null(warning.Version);
+        Assert.StartsWith("ESF2:", result.SourceDescription);
+    }
+
     [Fact]
     public async Task Ignores_effect_reference_records_that_reuse_cue_numbers_901_to_903()
     {
         var fixture = TestData.FixturePath(
-            "Esf3d",
-            "known-3.3.5_Build_69",
-            "synthetic-known-3.3.5_Build_69.esf3d");
+            "EosShowArchive",
+            "Compatibility",
+            "eos-3.3.5.69",
+            "parser-contract.esf3d");
         await using var stream = File.OpenRead(fixture);
 
         var result = await _sut.ImportAsync(new EosImportRequest(Path.GetFileName(fixture), stream),
@@ -102,13 +189,13 @@ public sealed class Esf3dEosCueImporterTests
     }
 
     [Theory]
-    [InlineData("known-3.3.5_Build_69-with-901", null)]
-    [InlineData("known-3.3.5_Build_69-with-901-label", "With Label")]
+    [InlineData("real-cue-901", null)]
+    [InlineData("real-cue-901-labelled", "test")]
     public async Task Keeps_a_real_cue_901_and_does_not_attach_global_effect_text(
         string fixtureDirectory,
         string? expectedLabel)
     {
-        var fixture = Directory.EnumerateFiles(TestData.FixturePath("Esf3d", fixtureDirectory), "*.esf3d").Single();
+        var fixture = Directory.EnumerateFiles(TestData.FixturePath("EosShowArchive", "Regression", fixtureDirectory), "*.esf3d").Single();
         await using var stream = File.OpenRead(fixture);
 
         var result = await _sut.ImportAsync(new EosImportRequest(Path.GetFileName(fixture), stream),
@@ -125,7 +212,9 @@ public sealed class Esf3dEosCueImporterTests
     public async Task Keeps_header_valid_cues_when_only_some_records_have_the_canonical_trailer()
     {
         var fixture = TestData.FixturePath(
-            "Esf3d",
+            "EosShowArchive",
+            "Regression",
+            "dense-cue-records",
             "dense-cue-record-variants-3.3.5.esf3d");
         await using var stream = File.OpenRead(fixture);
 
@@ -169,7 +258,7 @@ public sealed class Esf3dEosCueImporterTests
             TestContext.Current.CancellationToken);
 
         Assert.Empty(result.Cues);
-        Assert.Contains(result.Diagnostics, diagnostic => diagnostic is Esf3dNoCueSequenceWarning);
+        Assert.Contains(result.Diagnostics, diagnostic => diagnostic is EosShowArchiveNoCueSequenceWarning);
     }
 
     [Fact]
@@ -179,7 +268,7 @@ public sealed class Esf3dEosCueImporterTests
         var result = await _sut.ImportAsync(new EosImportRequest("empty.esf3d", archive),
             TestContext.Current.CancellationToken);
         Assert.Empty(result.Cues);
-        Assert.Contains(result.Diagnostics, d => d is Esf3dNoCueSequenceWarning);
+        Assert.Contains(result.Diagnostics, d => d is EosShowArchiveNoCueSequenceWarning);
         Assert.Contains(result.Diagnostics, d => d is NoCuesFoundWarning);
     }
 
@@ -187,7 +276,7 @@ public sealed class Esf3dEosCueImporterTests
     public async Task Chooses_the_shallowest_showdat_entry_case_insensitively()
     {
         var bytes = await File.ReadAllBytesAsync(
-            TestData.FixturePath("Esf3d", "known-3.3.5_Build_69", "synthetic-known-3.3.5_Build_69.esf3d"),
+            TestData.FixturePath("EosShowArchive", "Compatibility", "eos-3.3.5.69", "parser-contract.esf3d"),
             TestContext.Current.CancellationToken);
         await using var source = new ZipArchive(new MemoryStream(bytes), ZipArchiveMode.Read);
         var showData =
@@ -206,7 +295,7 @@ public sealed class Esf3dEosCueImporterTests
     {
         await Assert.ThrowsAsync<ArgumentNullException>(() =>
             _sut.ImportAsync(null!, TestContext.Current.CancellationToken));
-        await Assert.ThrowsAsync<Esf3dArchiveInvalidException>(() =>
+        await Assert.ThrowsAsync<EosShowArchiveInvalidException>(() =>
             _sut.ImportAsync(new EosImportRequest("bad.esf3d", new MemoryStream([1, 2, 3])),
                 TestContext.Current.CancellationToken));
         await using var noEntry = CreateArchive([], "other.dat");
@@ -217,6 +306,54 @@ public sealed class Esf3dEosCueImporterTests
                 TestContext.Current.CancellationToken));
     }
 
+    private static bool IsShowArchiveExtension(string extension)
+    {
+        return string.Equals(extension, ".esf3d", StringComparison.OrdinalIgnoreCase)
+               || string.Equals(extension, ".esf2", StringComparison.OrdinalIgnoreCase);
+    }
+
+    private static MemoryStream CreateArchiveWithVersion(byte[] showData, string versionJson)
+    {
+        var stream = new MemoryStream();
+        using (var archive = new ZipArchive(stream, ZipArchiveMode.Create, true))
+        {
+            var showDataEntry = archive.CreateEntry("showdat.dat");
+            using (var output = showDataEntry.Open())
+            {
+                output.Write(showData);
+            }
+
+            var versionEntry = archive.CreateEntry("version.json");
+            using var writer = new StreamWriter(versionEntry.Open());
+            writer.Write(versionJson);
+        }
+
+        stream.Position = 0;
+        return stream;
+    }
+
+    private static async Task<MemoryStream> CreateArchiveFromEntriesAsync(
+        ZipArchive source,
+        params string[] entryNames)
+    {
+        var stream = new MemoryStream();
+        using (var destination = new ZipArchive(stream, ZipArchiveMode.Create, true))
+        {
+            foreach (var entryName in entryNames)
+            {
+                var sourceEntry = source.Entries.Single(entry =>
+                    entry.FullName.Equals(entryName, StringComparison.OrdinalIgnoreCase));
+                var destinationEntry = destination.CreateEntry(entryName);
+                await using var input = sourceEntry.Open();
+                await using var output = destinationEntry.Open();
+                await input.CopyToAsync(output, TestContext.Current.CancellationToken);
+            }
+        }
+
+        stream.Position = 0;
+        return stream;
+    }
+
     private static string? GetOptional(JsonElement element, string name)
     {
         return element.TryGetProperty(name, out var value) ? value.GetString() : null;
@@ -238,4 +375,4 @@ public sealed class Esf3dEosCueImporterTests
         stream.Position = 0;
         return stream;
     }
-}
\ No newline at end of file
+}
diff --git a/tests/EosToQLab.Tests/Infrastructure/Import/Esf3d/Esf3dCueHeaderDecoderTests.cs b/tests/EosToQLab.Tests/Infrastructure/Import/Esf3d/Esf3dCueHeaderDecoderTests.cs
deleted file mode 100644
index c8286eb..0000000
--- a/tests/EosToQLab.Tests/Infrastructure/Import/Esf3d/Esf3dCueHeaderDecoderTests.cs
+++ /dev/null
@@ -1,122 +0,0 @@
-using EosToQLab.Tests.TestDoubles;
-
-namespace EosToQLab.Tests.Infrastructure.Import.Esf3d;
-
-public sealed class Esf3dCueHeaderDecoderTests
-{
-    [Theory]
-    [InlineData("F1", "F1")]
-    [InlineData("follow: 1.5", "F1.5")]
-    [InlineData("hang 2", "H2")]
-    [InlineData(" h3 ", "H3")]
-    public void Decodes_text_follow_variants(string input, string expected)
-    {
-        var fixture = Esf3dHeaderFixtureBuilder.Build(Esf3dHeaderFixtureBuilder.Text(input), "note");
-        var result = Esf3dCueHeaderDecoder.Decode(fixture.Data, fixture.CueNumberEnd, fixture.RecordEnd);
-        Assert.True(result.Parsed);
-        Assert.Equal(expected, result.Follow);
-        Assert.Equal(fixture.NotesOffset, result.CueNotesOffset);
-        Assert.NotNull(result.FollowTextOffset);
-        Assert.False(result.FollowDecodeFailed);
-    }
-
-    [Fact]
-    public void Decodes_unsigned_boolean_and_compact_object_variants()
-    {
-        Assert.Equal("F1.5", Decode(Esf3dHeaderFixtureBuilder.Unsigned(1500)).Follow);
-        Assert.Equal("H", Decode(Esf3dHeaderFixtureBuilder.Boolean(true)).Follow);
-        Assert.Equal("H1.5",
-            Decode([
-                0x02, .. Esf3dHeaderFixtureBuilder.Boolean(true), .. Esf3dHeaderFixtureBuilder.Unsigned(1500), 0x04
-            ]).Follow);
-        Assert.Equal("F1.5",
-            Decode([0x02, .. Esf3dHeaderFixtureBuilder.Unsigned(0), .. Esf3dHeaderFixtureBuilder.Unsigned(1500), 0x04])
-                .Follow);
-    }
-
-    [Fact]
-    public void Decodes_current_continuation_follow_and_hang_when_legacy_field_is_null()
-    {
-        var followFixture = Esf3dHeaderFixtureBuilder.Build(
-            [0x00],
-            continuation: Esf3dHeaderFixtureBuilder.Continuation(false, 1250));
-        var hangFixture = Esf3dHeaderFixtureBuilder.Build(
-            [0x00],
-            continuation: Esf3dHeaderFixtureBuilder.Continuation(true, 1500));
-
-        Assert.Equal(
-            "F1.25",
-            Esf3dCueHeaderDecoder.Decode(followFixture.Data, 0, followFixture.RecordEnd).Follow);
-        Assert.Equal(
-            "H1.5",
-            Esf3dCueHeaderDecoder.Decode(hangFixture.Data, 0, hangFixture.RecordEnd).Follow);
-    }
-
-    [Fact]
-    public void Decodes_real_eos_3_3_5_hang_continuation_bytes()
-    {
-        byte[] continuation =
-        [
-            0x02,
-            0x01, 0x00,
-            0x08, 0x01,
-            0x01, 0x00,
-            0x08, 0x01,
-            0x09, 0xDC, 0x05,
-            0x00,
-            0x08, 0x01
-        ];
-        var fixture = Esf3dHeaderFixtureBuilder.Build([0x00], continuation: continuation);
-
-        var result = Esf3dCueHeaderDecoder.Decode(fixture.Data, 0, fixture.RecordEnd);
-
-        Assert.True(result.Parsed);
-        Assert.Equal("H1.5", result.Follow);
-        Assert.False(result.FollowDecodeFailed);
-    }
-
-    [Fact]
-    public void Keeps_support_for_legacy_boolean_hang_mode()
-    {
-        var fixture = Esf3dHeaderFixtureBuilder.Build(
-            [0x00],
-            continuation: Esf3dHeaderFixtureBuilder.Continuation(
-                false,
-                1250,
-                secondLegacyMode: true));
-
-        var result = Esf3dCueHeaderDecoder.Decode(fixture.Data, 0, fixture.RecordEnd);
-
-        Assert.Equal("H1.25", result.Follow);
-    }
-
-    [Fact]
-    public void Null_follow_is_valid_and_unknown_encoding_is_reported()
-    {
-        var empty = Decode([0x04]);
-        Assert.True(empty.Parsed);
-        Assert.Null(empty.Follow);
-        Assert.False(empty.FollowDecodeFailed);
-
-        var unsupported = Decode([0x07]);
-        Assert.False(unsupported.Parsed);
-        Assert.True(unsupported.FollowDecodeFailed);
-    }
-
-    [Fact]
-    public void Invalid_header_is_unparsed()
-    {
-        var result = Esf3dCueHeaderDecoder.Decode([0x00], 0, 1);
-        Assert.Equal(Esf3dCueHeaderDecodeResult.Unparsed, result);
-        var withFollow = Esf3dCueHeaderDecodeResult.UnparsedWithFollow("F1", 3);
-        Assert.False(withFollow.Parsed);
-        Assert.Equal("F1", withFollow.Follow);
-        Assert.Equal(3, withFollow.FollowTextOffset);
-    }
-
-    private static Esf3dCueHeaderDecodeResult Decode(byte[] follow)
-    {
-        var fixture = Esf3dHeaderFixtureBuilder.Build(follow);
-        return Esf3dCueHeaderDecoder.Decode(fixture.Data, 0, fixture.RecordEnd);
-    }
-}
\ No newline at end of file
diff --git a/tests/EosToQLab.Tests/TestDoubles/Esf3dHeaderFixtureBuilder.cs b/tests/EosToQLab.Tests/TestDoubles/EosShowArchiveHeaderFixtureBuilder.cs
similarity index 97%
rename from tests/EosToQLab.Tests/TestDoubles/Esf3dHeaderFixtureBuilder.cs
rename to tests/EosToQLab.Tests/TestDoubles/EosShowArchiveHeaderFixtureBuilder.cs
index 47053bf..0317f09 100644
--- a/tests/EosToQLab.Tests/TestDoubles/Esf3dHeaderFixtureBuilder.cs
+++ b/tests/EosToQLab.Tests/TestDoubles/EosShowArchiveHeaderFixtureBuilder.cs
@@ -2,7 +2,7 @@ using System.Text;
 
 namespace EosToQLab.Tests.TestDoubles;
 
-internal static class Esf3dHeaderFixtureBuilder
+internal static class EosShowArchiveHeaderFixtureBuilder
 {
     public static (byte[] Data, int CueNumberEnd, int RecordEnd, int? NotesOffset) Build(
         byte[] followValue,
diff --git a/tests/EosToQLab.Tests/TestDoubles/FakeEosShowArchiveCompatibility.cs b/tests/EosToQLab.Tests/TestDoubles/FakeEosShowArchiveCompatibility.cs
new file mode 100644
index 0000000..66b85ee
--- /dev/null
+++ b/tests/EosToQLab.Tests/TestDoubles/FakeEosShowArchiveCompatibility.cs
@@ -0,0 +1,13 @@
+namespace EosToQLab.Tests.TestDoubles;
+
+internal sealed class FakeEosShowArchiveCompatibility : IEosShowArchiveCompatibility
+{
+    private readonly Func<string?, string?, bool> _isCovered;
+
+    public FakeEosShowArchiveCompatibility(Func<string?, string?, bool>? isCovered = null)
+    {
+        _isCovered = isCovered ?? ((_, _) => true);
+    }
+
+    public bool IsCovered(string? format, string? version) => _isCovered(format, version);
+}
diff --git a/tools/EosToQLab.CompatibilityCatalogGenerator/EosToQLab.CompatibilityCatalogGenerator.csproj b/tools/EosToQLab.CompatibilityCatalogGenerator/EosToQLab.CompatibilityCatalogGenerator.csproj
new file mode 100644
index 0000000..d7acbec
--- /dev/null
+++ b/tools/EosToQLab.CompatibilityCatalogGenerator/EosToQLab.CompatibilityCatalogGenerator.csproj
@@ -0,0 +1,7 @@
+<Project Sdk="Microsoft.NET.Sdk">
+  <PropertyGroup>
+    <OutputType>Exe</OutputType>
+    <TargetFramework>net10.0</TargetFramework>
+    <RootNamespace>EosToQLab.CompatibilityCatalogGenerator</RootNamespace>
+  </PropertyGroup>
+</Project>
diff --git a/tools/EosToQLab.CompatibilityCatalogGenerator/Program.cs b/tools/EosToQLab.CompatibilityCatalogGenerator/Program.cs
new file mode 100644
index 0000000..85f15fd
--- /dev/null
+++ b/tools/EosToQLab.CompatibilityCatalogGenerator/Program.cs
@@ -0,0 +1,178 @@
+using System.IO.Compression;
+using System.Text.Json;
+
+if (args.Length != 2)
+{
+    throw new ArgumentException(
+        "Usage: EosToQLab.CompatibilityCatalogGenerator <compatibility-fixture-root> <output-json>");
+}
+
+var fixtureRoot = Path.GetFullPath(args[0]);
+var outputPath = Path.GetFullPath(args[1]);
+if (!Directory.Exists(fixtureRoot))
+{
+    throw new DirectoryNotFoundException(
+        $"EOS show archive compatibility fixture root was not found: {fixtureRoot}");
+}
+
+var fixtureDirectories = Directory.EnumerateDirectories(fixtureRoot)
+    .OrderBy(path => path, StringComparer.Ordinal)
+    .ToArray();
+if (fixtureDirectories.Length == 0)
+{
+    throw new InvalidDataException(
+        $"No EOS show archive compatibility fixtures were found in '{fixtureRoot}'.");
+}
+
+var entries = new List<(string Format, string Version, string Fixture)>();
+foreach (var fixtureDirectory in fixtureDirectories)
+{
+    var fixtureName = Path.GetFileName(fixtureDirectory);
+    var expectedPath = Path.Combine(fixtureDirectory, "expected.json");
+    if (!File.Exists(expectedPath))
+    {
+        throw new InvalidDataException(
+            $"Compatibility fixture '{fixtureName}' has no expected.json contract.");
+    }
+
+    var archives = Directory.EnumerateFiles(fixtureDirectory)
+        .Where(path => IsShowArchiveExtension(Path.GetExtension(path)))
+        .OrderBy(path => path, StringComparer.Ordinal)
+        .ToArray();
+    if (archives.Length != 1)
+    {
+        throw new InvalidDataException(
+            $"Compatibility fixture '{fixtureName}' must contain exactly one .esf2 or .esf3d archive.");
+    }
+
+    var expectedIdentity = ReadExpectedIdentity(expectedPath, fixtureName);
+    var archiveIdentity = ReadArchiveIdentity(archives[0], fixtureName);
+    if (!IdentityEquals(expectedIdentity, archiveIdentity))
+    {
+        throw new InvalidDataException(
+            $"Compatibility fixture '{fixtureName}' declares " +
+            $"'{expectedIdentity.Format}'/'{expectedIdentity.Version}' in expected.json, " +
+            $"but version.json contains '{archiveIdentity.Format}'/'{archiveIdentity.Version}'.");
+    }
+
+    entries.Add((
+        archiveIdentity.Format,
+        archiveIdentity.Version,
+        fixtureName));
+}
+
+var duplicate = entries
+    .GroupBy(
+        entry => $"{entry.Format.Trim()}\u001f{entry.Version.Trim()}",
+        StringComparer.OrdinalIgnoreCase)
+    .FirstOrDefault(group => group.Count() > 1);
+if (duplicate is not null)
+{
+    throw new InvalidDataException(
+        $"More than one compatibility fixture covers the same EOS archive identity: " +
+        string.Join(", ", duplicate.Select(entry => entry.Fixture)));
+}
+
+var catalog = new
+{
+    schemaVersion = 1,
+    archives = entries
+        .OrderBy(entry => entry.Format, StringComparer.OrdinalIgnoreCase)
+        .ThenBy(entry => entry.Version, StringComparer.OrdinalIgnoreCase)
+        .Select(entry => new
+        {
+            format = entry.Format,
+            version = entry.Version,
+            fixture = entry.Fixture
+        })
+        .ToArray()
+};
+var json = JsonSerializer.Serialize(catalog, new JsonSerializerOptions
+{
+    PropertyNamingPolicy = JsonNamingPolicy.CamelCase,
+    WriteIndented = true
+}) + "\n";
+
+Directory.CreateDirectory(Path.GetDirectoryName(outputPath)!);
+if (!File.Exists(outputPath) || !string.Equals(File.ReadAllText(outputPath), json, StringComparison.Ordinal))
+{
+    var temporaryPath = outputPath + ".tmp";
+    File.WriteAllText(temporaryPath, json);
+    File.Move(temporaryPath, outputPath, true);
+}
+
+static (string Format, string Version) ReadExpectedIdentity(string expectedPath, string fixtureName)
+{
+    using var document = JsonDocument.Parse(File.ReadAllText(expectedPath));
+    var root = document.RootElement;
+    if (root.ValueKind != JsonValueKind.Object
+        || !root.TryGetProperty("archive", out var archive)
+        || archive.ValueKind != JsonValueKind.Object)
+    {
+        throw new InvalidDataException(
+            $"Compatibility fixture '{fixtureName}' must declare archive.format and archive.version in expected.json.");
+    }
+
+    return ReadIdentity(archive, "format", "version", $"expected.json of '{fixtureName}'");
+}
+
+static (string Format, string Version) ReadArchiveIdentity(string archivePath, string fixtureName)
+{
+    using var archive = ZipFile.OpenRead(archivePath);
+    _ = archive.Entries
+        .Where(entry => entry.FullName.EndsWith("showdat.dat", StringComparison.OrdinalIgnoreCase))
+        .OrderBy(entry => entry.FullName.Count(character => character == '/'))
+        .FirstOrDefault()
+        ?? throw new InvalidDataException(
+            $"Compatibility fixture '{fixtureName}' has no showdat.dat payload.");
+
+    var manifest = archive.Entries
+        .Where(entry => entry.FullName.EndsWith("version.json", StringComparison.OrdinalIgnoreCase))
+        .OrderBy(entry => entry.FullName.Count(character => character == '/'))
+        .FirstOrDefault()
+        ?? throw new InvalidDataException(
+            $"Compatibility fixture '{fixtureName}' has no version.json manifest.");
+
+    using var stream = manifest.Open();
+    using var document = JsonDocument.Parse(stream);
+    return ReadIdentity(document.RootElement, "Format", "Version", $"version.json of '{fixtureName}'");
+}
+
+static (string Format, string Version) ReadIdentity(
+    JsonElement element,
+    string formatPropertyName,
+    string versionPropertyName,
+    string source)
+{
+    if (element.ValueKind != JsonValueKind.Object
+        || !element.TryGetProperty(formatPropertyName, out var formatProperty)
+        || formatProperty.ValueKind != JsonValueKind.String
+        || !element.TryGetProperty(versionPropertyName, out var versionProperty)
+        || versionProperty.ValueKind != JsonValueKind.String)
+    {
+        throw new InvalidDataException($"{source} has no readable format/version identity.");
+    }
+
+    var format = formatProperty.GetString()?.Trim();
+    var version = versionProperty.GetString()?.Trim();
+    if (string.IsNullOrWhiteSpace(format) || string.IsNullOrWhiteSpace(version))
+    {
+        throw new InvalidDataException($"{source} has an empty format/version identity.");
+    }
+
+    return (format, version);
+}
+
+static bool IdentityEquals(
+    (string Format, string Version) left,
+    (string Format, string Version) right)
+{
+    return StringComparer.OrdinalIgnoreCase.Equals(left.Format, right.Format)
+           && StringComparer.OrdinalIgnoreCase.Equals(left.Version, right.Version);
+}
+
+static bool IsShowArchiveExtension(string extension)
+{
+    return string.Equals(extension, ".esf2", StringComparison.OrdinalIgnoreCase)
+           || string.Equals(extension, ".esf3d", StringComparison.OrdinalIgnoreCase);
+}
